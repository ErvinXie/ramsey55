# 2026-08-23

- Closed the formal vertex-relabeling gap.  `Relabeling.lean` constructs the
  deterministic selected-vertex/neighbour/nonneighbour order, proves it is a
  permutation of all labels, and transports simple colourings and arbitrary
  distinct five-tuples through injective relabelings.  The bridge to the
  project's historical increasing-five-tuple predicate sorts the five labels
  with `mergeSort` and transports monochromaticity through the permutation.
- Extended `Order45.lean` with degree preservation under relabeling, exact
  neighbour/nonneighbour positions in the fixed-star order, and
  `order45_fixedStar_normalize_of_window`.  Given the 20--24 window, Lean now
  returns exactly the degree-20 or degree-22 fixed-star branch consumed by the
  SAT encodings.  Targeted ARM builds pass; the axiom audit contains only
  standard `propext`, `Quot.sound`, and `Classical.choice`, with no `sorryAx`.
- Added `Order45Window.lean`.  It isolates the external upper half of
  `R(4,5)=25` as `ForcesRed4OrBlue5 25`, uses the first 25 vertices in the
  checked neighbour order to prove the degree upper bound 24, and applies the
  same argument to the complemented colouring for the lower bound 20.
  `order45_fixedStar_normalize_of_r45` therefore leaves the formal graph-side
  reduction with that single explicit order-25 theorem input.  Its targeted
  ARM build and three axiom audits pass without `sorryAx`.  After syncing the
  new root import, the complete pinned ARM Lean project passes all 81 jobs.
- The final direct v399 J297 f31s1 child completed after 383 attempts and 191
  splits, making all three children available.  Stopped four now-redundant
  v408 row-2 descendant races and retained their partial proofs.
- The production finalizer rejected the raw v399 proof prefix because a
  `SIGSTOP` snapshot ended 20 bytes into a binary DRAT clause.  Added
  `frame_binary_drat_prefix.py`, which preserves and hash-binds the immutable
  source, truncates only after the last complete NUL-terminated clause, emits
  a derived replay manifest, and records the tail hash.  Original/framed
  prefix hashes are `62f71019...175eeca` / `9be57352...2289eb`; the framed
  replay hash is `4db8ab2a...125e0`.  A scan found 1--169-byte tails in many
  other historical stopped snapshots, so the replay tool now rejects an
  unframed supplied prefix and computes large-file hashes by streaming.
  Thirteen focused framing/replay/finalizer tests pass on ARM.
- Removed two orphaned, obsolete v203/v204 screening `cadical` processes
  whose parents had exited and whose output pipes had no consumer.  Reused
  the cores for proof-producing seeded races.  v406 f321 row 0 subsequently
  closed under two original strategies; its two remaining redundant seeds
  were stopped, and capacity was reassigned across open v403/v404/v406/v407/
  v409 blockers.  The machine is again saturated at 63 proof producers plus
  the v399 checker.
- The first deletion-aware check of the 7,271,596,689-byte v399 standalone
  composition returned `s NOT VERIFIED` at line 102,694,099 inside the
  third child; its retained log hash is `cb58113b...b1fd0`.  Independent
  fragments carry deletion streams from separate solver states, so the
  finalizer now records checker options and is rerunning with `drat-trim -p`,
  which ignores deletions but still checks every addition.  Fourteen focused
  tests pass.  No fragment is accepted before `s VERIFIED` and an atomic
  finalization manifest.
- v407 f220 row 1 completed after 297 attempts/148 splits, making that group
  2/2; the f221 strong one-row proof completed after 31 attempts/15 splits.
  Stopped seven redundant races over those exact rows.  Their 21-byte and
  6-byte raw-prefix tails were framed with hashes
  `3d0cf1ae...982ea7` and `159cfb03...2ea962`; both groups are undergoing
  independent plain-mode finalization in parallel with v399.
- v406 f320 row 1 completed under the primary route after 291 attempts/145
  splits, making the group 2/2.  Stopped its four remaining redundant races.
  Framing removed the raw prefix's four-byte incomplete tail; the framed
  prefix/replay hashes are `35ede3ca...1dd37da` / `e85e1564...f4a5066`.
  A fourth plain-mode finalization is in progress.  v403 f21 row 0 also
  completed under the high route after 33 attempts/16 splits, leaving f21 at
  1/2; stopped three redundant row-0 races and retained partial artifacts.
- The standard README Python command with `PYTHONPATH=src:tools` passes all
  205 tests on ARM in 38.571 seconds.  The retained log hash is
  `3f6cb9e8...e5a33b3`.  Earlier test-discovery invocations with incomplete
  import paths were environment errors and did not fail functional tests.
- After completed-row races were stopped and older timed runs expired, three
  batches of eight proof-producing races restored useful ARM load to about
  59/64.  The first two batches use phase 1 with seeds 283--337 and 347--383;
  the final orthogonal batch uses phase 0 with seeds 389--433.  They cover
  only the open v403 f10/f21-row-1, v404 row 1, v406 f321-row-1/f331 rows,
  v407 f222 rows 2/3, and v409 row 1 leaves.  Every output stem was checked
  for nonexistence before launch; no new `status=20` result has appeared yet.
- The four deletion-ignoring checkpoint checks remained CPU-bound for more
  than 1.5 hours with stable memory.  Started a separate forward-mode
  finalization race for v407 f221 with recorded options `-p -f`, then a
  second for f220 after the first forward checker had run for 30 minutes.
  They use distinct proof, log, and manifest paths; neither forward nor
  default-mode run has produced an acceptance or rejection yet.
- Added `--drop-deletions` to the checkpoint finalizer.  It emits a monotone
  addition-only binary DRAT composition and records the mode plus exact
  output/standalone clause counts.  A real ARM checker regression deletes one
  unit from a contradictory-unit CNF, confirms the normalized fragment is
  empty, appends the final empty addition, and is accepted by ordinary
  `drat-trim` without `-p`.  Ten focused finalizer tests and the complete 207
  test suite pass; the full log hash is `151b3a62...08733d9`.
- Started ordinary `drat-trim` checks of deletion-normalized v407 f221/f220,
  v399 f31s1, and v406 f320 compositions on ARM cores 57--54.  Their no-empty
  fragment sizes are respectively 1,969,952,579, 2,308,740,618,
  3,665,553,914, and 2,147,961,508 bytes.
  Six older `-p` default/forward checks were then terminated after exact PID
  and command validation because deleting the ignored deletion records makes
  them semantically redundant with these standard-checker runs.  No accepted
  artifact or original proof was removed; roughly 23 GiB of resident memory
  was released.
- Added `audit_cadical_dfs_checkpoint_finalization.py`, an independent
  recursive auditor for finalization manifests.  It recomputes file hashes and
  binary clause counts in one streaming pass, reconstructs the exact ordered
  component digest (with physical deletion filtering when declared), checks
  that standalone is precisely fragment plus `a\0`, validates producer or
  finalized-child evidence, and can rerun the recorded checker.  Its four
  focused ARM tests include rehashed-output tampering and recursive child
  composition.  The complete suite now passes 211 tests in 40.843 seconds;
  the retained log hash is `53312690...79c5661`.
- Added the internal `RAMSEY55_CADICAL_WALL_SECONDS` checkpoint deadline to
  `prove_cadical_cubes.cpp`.  When it expires, the runner leaves an incomplete
  solve out of the TSV, actively flushes/closes the no-empty DRAT stream, and
  reports `checkpoint=1`, `status=0`; replay therefore retains the current
  cube instead of trusting a partial row.  The ARM proof smoke suite passes
  with the new immediate-checkpoint case.  A five-second real v407 f222 row-2
  test wrote a 1,942,705-byte nonempty prefix ending in NUL and replayed the
  exact one-root frontier.  Built `prove_cadical_cubes_seeded_v7` for future
  jobs; the live v6 jobs were not disturbed.
- Added `Order45Target.lean`.  It names the exact fixed-star graph-to-CNF
  completeness predicate, proves that completeness plus CNF UNSAT excludes a
  branch, and derives `ForcesMonochromatic5 45` either from the two d20/d22
  mother-formula refutations or directly from formula-relative cube covers
  and all covered leaf refutations.  The full ARM root build passes 82/82
  jobs; the new axiom audits contain only standard Lean axioms and no
  `sorryAx`.  `R(4,5)=25`, concrete encoding completeness, and computed leaf
  UNSAT remain explicit inputs rather than claimed results.
- Preserved the expiring v403 f10 seed151/p0 race as v410: 396 complete rows
  replay to three open cubes.  Its 4,724,830,208-byte raw proof hash is
  `e96dc2ed...56eb3`; framing removed a 208-byte tail and produced hash
  `95cc6921...8ab805` plus framed-replay hash `e8a2e2c2...ab20f`.  Three v7
  children started, and row 0 closed in one attempt.
- Eight more v6 four-hour races expired without closing their roots.  The
  duplicate seed167 f10 route was skipped; the other seven were replayed and
  framed as v411--v417 with 2/2/3/4/3/2/3 open rows.  Their incomplete tails
  were 42/3/117/62/90/24/53 bytes and all hashes are recorded in the stable
  order-45 note.  Launched all 19 disjoint rows with v7 and the internal
  14,400-second deadline on cores 0--53.  v411 row 0, v412 row 0, v414 row 2,
  and v417 row 0 closed within the first minutes.  Added exactly one
  opposite-phase race for each sole remaining v411/v412 row on cores 61--62;
  no group is yet promoted.
- v414 row 0 closed after seven attempts/three splits and v415 row 0 closed
  after fifteen attempts/seven splits, advancing those groups to 2/4 and 1/3.
  A bounded 1,800-second `drat-trim -U` diagnostic on the v407 f221
  addition-only proof neither accepted nor rejected before timeout, so it
  provides no evidence of a faster checking mode and was not adopted.
- Recomputed the six expired phase-1 comparison frontiers directly from their
  flushed DFS rows.  Their open counts are 2/2/2/3/4/2 for v406 f321 row 1,
  v406 f331 rows 1/2, v407 f222 rows 2/3, and v404 row 1.  These are equal to
  or worse than the active routes' unfinished counts 2/2/2/3/2/1, so no
  prefix was copied and no redundant child jobs were started.
- Removed the finalization auditor's imports from the production finalizer.
  It now has independent manifest validation, producer-log parsing, hashing,
  and expected-clause aggregation code.  A new isolated-distribution test
  runs a copied auditor under Python `-I`, where the finalizer module is not
  available.  Five focused tests pass locally; the full ARM suite passes all
  212 tests in 40.013 seconds on logical core 63.
- Pinned the published HOL4 `R(4,5)=25` source at upstream commit
  `065c07054483e3132f12909103e6d0e35e912c28`.  The final theorem script and
  generic reduction blobs are `5e211cf6...8307ff0` and
  `69b249e6...31540d`.  The source explicitly derives `RAMSEY 4 5 25`, but
  remains an external formal input to Lean.  Upstream documents roughly
  500 GB RAM for its enumeration stage and weeks for the gluing stages, so
  the 244 GiB ARM host is kept on the new order-45 computation instead of
  redundantly rebuilding that result.
- Corrected the end-to-end formal interface for the actual structural proof.
  A single `FixedStarCnfComplete` formula cannot directly represent a chosen
  lex/fixed-pair case.  `FixedStarCnfFamilyComplete` instead requires every
  fixed-star colouring to reach some member of a finite formula family, and
  the two new order-45 theorems combine family completeness with either per-
  formula UNSAT or per-formula relative cube covers and leaf refutations.
  Targeted Lean and the complete 82-job root build pass; axiom audits contain
  only `propext`, `Classical.choice`, and `Quot.sound`.
- v410 row 1 closed after 101 attempts/50 splits, advancing that checkpoint
  to 2/3.  Used newly idle capacity for twelve opposite-phase v7 races on the
  still-open v410/v413--v417 rows; an intended thirteenth launch safely
  skipped the newly closed v410 row.  v416 row 0 then closed after 97
  attempts/48 splits, bringing that group to 1/2.  Its exact redundant
  seed661/p1 process was terminated and the 267,636,736-byte partial proof
  retained.
- Added `compose_binary_drat_protect_cnf.py`, a checker-gated experiment that
  omits only deletion instructions targeting canonical clauses from the
  mother CNF while retaining learned-clause deletions.  It hash-binds every
  source/output and rejects embedded empty additions.  Four focused ARM tests
  include a genuine `drat-trim` regression.  The complete suite passes 216
  tests in 41.847 seconds; log SHA-256 is
  `58db0b5456101bde0ca66b6b654a3405a2c200098286f597360b9b652d3ffe00`.
  The full f221 experiment emitted a 3,916,568,669-byte proof with 61,659
  protected deletions omitted and SHA-256 `b13e0b6d...c8ff0e`; its standard
  checker was still running at that checkpoint, so no finalization claim used
  it then.
- Added `Order45Excess.lean` and imported it from the root project. It proves
  the global excess identity by an ordered-triple `2 - 1 - 1` cancellation,
  extracts a nonpositive vertex, proves colour-complement invariance of the
  local score, and normalizes an order-45 Ramsey-free witness in the 20--24
  window to degree 20, 21, or 22. It now also proves relabelling invariance,
  moves the witness to the fixed apex, constructs the concrete H/J local
  graphs, obtains their natural edge counts by handshake, and proves the
  score decomposition. Thus the kernel derives the exact 226/222/220 dense
  branches from `ForcesRed4OrBlue5 25` alone. The complete 83-job ARM build
  log SHA-256 is
  `6f08bf693dd183ca19b7f1bf7bf0d218b13ea5a5b5e4954071c445eeca1ca551`;
  axiom audits contain only `propext`, `Classical.choice`, and `Quot.sound`.
  The H/J-primary-variable and DIMACS-counter bindings remain open; no UNSAT
  result is inferred.
- Added `Order45ExcessTarget.lean`. Its abstract theorem composes complete
  d20/d21/d22 dense-excess mothers with their UNSAT facts; its certificate
  theorem accepts formula-relative cube covers and leaf refutations; and its
  concrete theorem fixes the generated 28/36/45 typed DIMACS cube lists plus
  the checked counter-tail inclusions. This is the global structural route,
  unlike the local H100/J132 fixed-pair computation. The complete 84-job ARM
  build passes; log SHA-256 is
  `55e644b60ef7af1f1371b69fdd16b4942414e59be6b613ab1f42c3a2a41d0390`.
  All new axiom audits contain only `propext`, `Classical.choice`, and
  `Quot.sound`; full mother completeness and every leaf UNSAT remain open.
- Added `Order45LocalCatalog.lean`. It proves that the fixed-star neighbour
  block is a labelled `R(4,5,d)` graph and that the complemented nonneighbour
  block is a labelled `R(4,5,44-d)` graph. Published edge ranges are isolated
  as `Ramsey45EdgeRange` inputs, and the theorem derives bounds on exact
  induced H/J edge counts. The initially explicit count-level link,
  `Order45LocalCatalogCountBinding`, has since been proved. Generic prefix and
  suffix padding lemmas show that adding isolated labels preserves the degree
  sum; fixed-star pointwise lemmas then identify the padded catalog H/J graphs
  with the two local graphs used by the excess identity. A brute-force
  45-by-45 simplification was stopped after roughly 90 seconds; the permanent
  proof instead uses these structural padding lemmas. The final complete
  85-job ARM build passes without linter warnings in the new module; log
  SHA-256 is
  `9e6fd5d1e37e0e206780fb78f95d2d8cb88775c7305e70d303edfc4b75770861`.
- Factored the final excess target through
  `CatalogBoundedDenseExcessCnfComplete`. The proved local-catalog count
  binding now upgrades an encoder that is complete only inside its exact H/J
  ranges to `DenseExcessCnfComplete`. The five order-20 through order-24
  published range statements are exposed once as
  `Order45ExcessCatalogRanges`, and the new concrete theorem composes that
  boundary with the 28/36/45 DIMACS cube families. The remaining encoder,
  range-classification, and leaf-UNSAT obligations stay explicit. The full
  85-job ARM build passes; log SHA-256 is
  `af81065a83c612ad773a24552dc8122eb9f0cc22484cec87d4639d33f2f344fd`.
- Added `Order45Primary.lean`. A canonical strict-upper-triangle stream now
  has a proved handshake identity and a proved recursive correspondence with
  `orderedPairsFrom`. `RepresentsOrder45Primary` connects triangular DIMACS
  identifiers to a graph colouring; its positive H inputs and negative J
  inputs are proved to count the exact excess-identity edges. The concrete
  d20/d21/d22 theorems close all six 190/276, 210/253, and 231/231 input-count
  equalities. The full 86-job ARM build passes; log SHA-256 is
  `f3bad96f524e36f8722e776cadfb9ed7062b9198aab14a02532b7b75658902dd`.
  Constructing a representing assignment satisfying common clauses, counter
  auxiliaries, and lex clauses remains open.
- Closed the graph-primary assignment gap in `Order45Primary.lean` without a
  computational axiom. An initial `native_decide` experiment compiled in
  about two seconds but its axiom audit exposed a generated native-decision
  axiom, so it was rejected. The permanent proof establishes duplicate-free
  `orderedPairsFrom` rows structurally, identifies the DIMACS formula with a
  recursive triangular offset, and separates distinct second-endpoint rows by
  disjoint integer intervals. `order45GraphPrimaryAssignment` now assigns all
  990 primaries for any colouring and proves `RepresentsOrder45Primary`.
  Representation-free d20/d21/d22 corollaries then bind the concrete counter
  inputs directly to the excess H/J counts. The full 86-job ARM build passes;
  log SHA-256 is
  `fce950b20f5f4375eb054ffcf88abe183e9a2df0d40fa3cb78c7f6b4c6512eb9`.
  Its new axiom audits contain only `propext`, `Classical.choice`, and
  `Quot.sound`. Five long-running standard `drat-trim` checks remain active
  after roughly 1h51m--3h48m with no accept/reject result, so no UNSAT claim is
  made.
- Proved the missing generic sequential-counter completeness direction in
  `CnfCardinality.lean`. Intended prefix-threshold truth values now imply all
  initial, first-column, diagonal, and interior clauses in the exact emitted
  row-major stream. Converse theorems for range units and truncated dense-sum
  clauses show that in-range H/J counts satisfying the excess threshold model
  the complete suffix. `satisfiesSequentialCounterPairEncoding_of_exact`
  packages the two state grids and all constraints into one result. The full
  86-job ARM build passes; log SHA-256 is
  `bd6215dc905d48c58587012acd649bed53813283507b14c8714ff96d444a9113`.
  All added audits contain only `propext`, `Classical.choice`, and
  `Quot.sound`. The remaining concrete counter step is finite assignment over
  the two disjoint state-variable intervals, not counter-clause semantics.
- Added `Order45CounterAssignment.lean` and closed that concrete step. A
  recursive row-major coordinate list is proved duplicate-free; monotonic
  `counterCellsBefore` intervals make every state identifier injective; and
  the H/J tables are proved disjoint at their exact generator bases. A finite
  overlay writes intended prefix-threshold values while preserving all graph
  inputs below the counter base. The concrete
  `order45Degree20/21/22CounterTail_satisfied` theorems construct assignments
  satisfying both counter streams, four range units, and every dense-sum
  clause from the graph-side exact counts and bounds. The full 87-job ARM
  build passes; log SHA-256 is
  `98265b66bdff8dd64dd898866ecf067f5a08271960d5c3e5a6e03473bb92b753`.
  There is no `sorryAx` or native-decision axiom. Mother completeness now
  remains at the Ramsey/fixed-star/degree-window/lex prefix and exact suffix
  inclusion boundary, not at either primary or counter assignment.
- Extended the counter assignments with proofs that all 990 primaries remain
  represented, then added `Order45MotherPrefix.lean`. The latter formalizes
  the exact ten-literal pair order for each increasing five-set and proves
  both signed Ramsey clauses from `IsRamseyFree55`; it also proves every
  fixed-star unit from `HasFixedStar`. Per-clause formula-shape predicates keep
  the 2,443,518-clause prefix out of kernel evaluation while exposing its data
  binding explicitly. The full 88-job ARM build passes; log SHA-256 is
  `f9b24a10bd091feb9e8b4787067a52fb0b873d43d44573847c8317de523cc9c7`.
  No `sorryAx` or native-decision axiom appears. The remaining mother prefix is
  the 44 degree-window counters, cross-row lex auxiliaries, and exact DIMACS
  stream shape/inclusion.
- Added `Order45DegreeWindowAssignment.lean` and closed the complete non-lex
  mother-assignment layer. The generator's increasing-label incident stream is
  proved to count the graph degree after deleting the false diagonal entry.
  Forty-four 44-by-25 truncated counters are constructed in disjoint
  800-variable blocks, exactly covering identifiers 991--36190, while
  preserving all 990 graph primaries. Their row-major cell clauses and
  `[20,24]` units are satisfied from `ForcesRed4OrBlue5 25`. Overlaying the
  existing H/J assignments then yields concrete d20/d21/d22 assignments that
  satisfy the Ramsey-shape interface, fixed star, all degree windows, and the
  complete H/J tail under the already explicit local count/range/density
  premises. Only the cross-row lex block is deleted in the new non-lex formula
  theorems. The full 89-job ARM build log SHA-256 is
  `6910fd89e57a34a301ecd71f9caaaa7236381f7dd3920656a1be24202765df4b`;
  every new audit contains only `propext`, `Classical.choice`, and
  `Quot.sound`. Lex/DIMACS stream binding, external `R(4,5)=25` and catalog
  ranges, and all leaf UNSAT certificates remain open. The five long standard
  `drat-trim` checks are still running after about 2h35m--4h31m with no output
  or terminal result, so they provide no new certificate claim.
- Added `CnfLex.lean` and `Order45LexAssignment.lean`. The first reproduces
  `lex_leq_encoding` clause-for-clause and proves constructive completeness of
  its guarded order and equality-prefix clauses. The second constructs all
  order-45 lex states above the degree counters, proves their keys disjoint,
  and kernel-checks the d20/d21/d22 endpoints 36627/36630/36631 and clause
  counts 2622/2640/2646. The lex overlay preserves all primaries and all 44
  degree counters; under the explicit adjacent-row sorting predicate it
  satisfies the full lex formula. The complete 91-job ARM build log SHA-256 is
  `56ea53bcee64b56da08aabf9a3c78c774800af7c6d7046f1467697b3f263df97`,
  with no `sorryAx` or native-decision axiom. Still open are the vertex-
  relabeling theorem that always realizes the sorting predicate, H/J overlay
  preservation above the filled lex source, and exact generated-stream
  inclusion.
- Completed the typed full-mother construction. `Order45LexAssignment.lean`
  now overlays the d20/d21/d22 H/J state tables on the graph, degree, and lex
  assignment and proves satisfaction of the exact five-block order Ramsey /
  fixed star / 44 degree windows / lex / counter tail. Added
  `Order45LexRelabeling.lean`: it merge-sorts fixed-length 0/1 cross-row keys,
  proves the induced neighbour permutation extends to a 45-vertex relabeling
  fixing zero and every nonneighbour, and proves that relabeling preserves
  simplicity, Ramsey-freeness, fixed star, both H/J counts, and the local
  excess score. The three resulting completeness theorems discharge
  `CatalogBoundedDenseExcessCnfComplete` for the typed d20/d21/d22 mothers,
  with no remaining assumed lex predicate. The complete 92-job ARM build
  passed; log SHA-256 is
  `70c94b5697a895c9960598daf98916db43435fd3baf7f1233cdcdd3585f0b120`.
  Axiom audits contain only `propext`, `Classical.choice`, and `Quot.sound`.
  Exact generated-DIMACS stream binding, the external `R(4,5)=25` and five
  catalog ranges, and all 109 leaf UNSAT certificates remain open.
- Replaced the last abstract mother-prefix parameters with concrete typed
  streams. `Order45MotherPrefix.lean` now defines an include-first exact-
  cardinality sublist enumerator matching `itertools.combinations`, proves
  every enumerated five-set is an increasing sublist of `range 45`, and uses
  it to define the negative-then-positive Ramsey clause stream. It also
  defines the exact 44 fixed-star units. The d20/d21/d22 exact full mothers
  and their premise-free prefix completeness theorems now live in
  `Order45LexAssignment.lean` and `Order45LexRelabeling.lean`. The complete
  92-job ARM build passes; log SHA-256 is
  `d4433f785b3280bf4f35123530f4be9f090f9b83ef100b4cc49cc6034439616c`,
  and all new audits contain only `propext`, `Classical.choice`, and
  `Quot.sound`.
- Added `tools/VerifyOrder45ExactMothers.lean`, an executable check directly
  compiled from those typed definitions. On ARM it converted every literal
  back to its signed DIMACS integer and compared all 2,751,846 / 2,745,658 /
  2,743,672 clauses of the d20/d21/d22 files in order. The clean 147.99-second
  run used 5,960,796 KiB peak RSS and its log hashes to
  `a7e0cbe9ce5adf4cf38a4813a12856cc344690cd3044961d2e6e767b83f4f275`.
  The independent Python reconstruction also re-passed in 24.67 seconds with
  log SHA-256
  `ad51396add1618d2be8b5bcfab8a32727fe7c4177d5470357ecc3b78071f7f47`.
  Thus the concrete typed mothers are now checked clause-for-clause against
  the hash-bound external files; the remaining mathematical/certificate
  boundaries are `R(4,5)=25`, the five catalog edge ranges, and all 109 leaf
  UNSAT proofs.

No parent-1 UNSAT, fixed-pair UNSAT, order-45 UNSAT, or `R(5,5) <= 45`
theorem is claimed.
- Added a direct, proof-oriented `R(4,5,25)` path. The generators emit the
  300-variable / 65,780-clause base formula and all 25 fixed-star branches,
  each with 65,804 clauses. The independent Python reconstruction passed; the
  base CNF SHA-256 is
  `bb9c8545a66fc0a9b6a80f5786d325d8262d14fd7339d69c6f87960985d9d690`
  and the branch-manifest hash is
  `7a88819560bc19bbd4e4696e81b46000949911db8b969c0d272d54fac716533e`.
  Solver scouting without proof closed 17 branches (degree 21 took 88
  seconds); degrees 3 and 7--13 timed out under the tested lex, fix, and
  random/Schreier symmetry-break variants. These runs are strategy data, not
  UNSAT evidence.
- Added `Ramsey45Target.lean`. It proves injective relabeling invariance, the
  exact 0--24 degree cover, graph-to-CNF completeness of every exact branch,
  and `forcesRed4OrBlue5_of_exactFixedStarUnsat`: 25 branch UNSAT facts now
  suffice for `ForcesRed4OrBlue5 25`. The new axiom audits contain only
  `propext`, `Classical.choice`, and `Quot.sound`. The typed executable
  `VerifyRamsey45ExactBranches.lean` compared every one of the 1,645,100
  generated branch clauses in order on ARM in 9.54 seconds with 535,764 KiB
  peak RSS; log SHA-256 is
  `5ba01356fc090a96e9401224527711e41842e9648b6ade40c61cd36fc820fba5`.
  The remaining boundary for this unoptimized route is exactly the 25 UNSAT
  certificates.
- Formalized the classical three-degree optimization. New predicates package
  `R(3,5) <= 14` and `R(4,4) <= 18`; checked neighbourhood/nonneighbourhood
  arguments force every hypothetical `R(4,5,25)` degree into 7--13, and the
  25-vertex handshake parity proof selects degree 8, 10, or 12 after
  fixed-star relabeling. The theorem
  `forcesRed4OrBlue5_of_threeExactFixedStarUnsat` now needs only those two
  smaller Ramsey inputs and exact UNSAT certificates for d08/d10/d12. Its
  axiom audit contains only `propext`, `Classical.choice`, and `Quot.sound`.
  This matches the three gluing cases in the published HOL4 proof; the three
  certificates and two smaller checked inputs remain open here.
- Added generic direct asymmetric-Ramsey CNF generation and a separate
  reconstructor. The audited `R(3,5,14)` input has 91 variables / 2,366
  clauses and SHA-256
  `14125c1fcc0103d79cdf143cea36244d7343421b08cd671cbf918250875989c7`;
  `R(4,4,18)` has 153 variables / 6,120 clauses and SHA-256
  `e75cd6d1d26230aec73320b29f85162b162ff52e25942a6d6847742a4e82b55b`.
  Bounded raw Kissat proof runs were started in `/dev/shm`; no result is
  recorded until both solver termination and `drat-trim` acceptance occur.
- The f221 protected-CNF proof passed ordinary `drat-trim` after 12,823.761
  seconds. The 3,916,568,669-byte proof has SHA-256
  `b13e0b6d5b826898657344feb53b92f301dc97025afaba12f9dc435f60c8ff0e`;
  the checker log hashes to
  `b80cd5810ac6ad45c8dbc7df706d53399b473a2d6da0d8954ff1c03fcc5ec50b`.
  Added an independent auditor that never imports the producer and protects
  exact canonical CNF tuples instead of short fingerprints. It reconstructed
  both source fragments, all 29,631,565 additions, 29,077,845 retained
  deletions, 61,659 omitted original-clause deletions, and the final empty
  clause, then matched the output hash and bound checker status. The full
  audit passed in 608.22 seconds with 112,364 KiB peak RSS; log SHA-256 is
  `5e42086bdc1c518afc705c0f4d159ad9ece5eec5cb0d792fdbd5c4b414e4cbd3`.
  This establishes UNSAT only for the f221 augmented CNF; the other f22
  children and every enclosing proof level remain open.
- The complete incremental ARM Lean build passed 93 jobs (log SHA-256
  `a14fa295ac1732c7e0f753cdbcf80e027b4eb9df8e4cb456965dab97e67c4f0e`).
  The full Python suite passed all 223 tests in 23.480 seconds (log SHA-256
  `ddcb2a6d150b65d5cb9980aa8747cead1f8cf5022080143c748490dd0f37bdba`).
- Added a checker-evidence promotion layer for the verified f221
  protected-CNF proof. A fresh exact source audit passed in 607.50 seconds;
  its pure JSON SHA-256 is
  `8475acc230842b59b97ebf5b48ba83579de508e19d375b949cfb21142f7438e4`.
  The promotion removes exactly the final binary `a\0`, producing a
  3,916,568,667-byte no-empty fragment with SHA-256
  `58a3c0f1a4c14c56dd80a041ef0e78d59cf8d85997331b245addde27ac495c03`.
  Its 29,631,564 additions and 29,077,845 deletions are now accepted by the
  recursive checkpoint finalizer only through the new strict manifest
  schema. A separate auditor scans both outputs and can rerun the exact source
  audit. This makes f221 embeddable but does not close f220, f222, or f22.
- Replaced two poor small-bound certificate targets with one tiny exact
  target. Raw unsymmetrized R(3,5,14) and R(4,4,18) Kissat runs both timed out
  at 1,800 seconds, leaving 1.1/1.2 GiB incomplete traces. R(3,4,9) instead
  has 36 variables and 210 clauses; Kissat solved it in 0.13 seconds and
  ordinary `drat-trim` verified the 210,962-byte proof in 0.149 seconds with
  no core RAT lemma. The CNF/proof SHA-256 values are
  `1c5d12b0f0b76943d2c0ac750c158ac94939009a3fa55924aa8be4418f2bd2c4`
  and
  `6410b4135b83c8040024d32688b453954447f71ef7fb704d5f235041394ae2c6`.
  The retained all-layer audit report hashes to
  `93d2ac434d6a4623c2588497805222a19899c96ee894bb677e7269861a92b4e5`.
- Added `RamseySmallBounds.lean` and `Ramsey34Target.lean`. Lean now proves
  R(3,4)<=9 implies both R(3,5)<=14 and R(4,4)<=18, proves completeness of the
  exact retained 210-clause formula, and reduces the three-degree R(4,5,25)
  theorem to this discharged small certificate plus d08/d10/d12 UNSAT. The
  typed formula matched all DIMACS lines. New axiom audits contain only
  `propext`, `Classical.choice`, and `Quot.sound`.
- The complete ARM build passed 95 jobs; log SHA-256 is
  `e095dde784f4d6062277f93b24d7208cd50ab8aa4a7d25d3d8340e3d363571d2`.
  The full Python suite passed all 232 tests in 24.836 seconds; log SHA-256 is
  `f6c671bf20495f95e68ad8d79bdee210167ee730f519d5c91a3b26359b30b895`.
- Completed the independent f221 promotion audit with a full source-audit
  rerun. It passed in 653.48 seconds with 113,860 KiB peak RSS. The audit
  report SHA-256 is
  `3d8482ad9abfb63c4184c62d0867035dda3447170ff7f665a7b6548bb99fc93a`;
  its timing-log SHA-256 is
  `fa57af832c39e4f82ac8e9345b52751718de409c471376975b2e954540c55000`.
  The rerun reproduced source-audit hash
  `8475acc230842b59b97ebf5b48ba83579de508e19d375b949cfb21142f7438e4`
  and accepted the exact promoted-fragment hash
  `58a3c0f1a4c14c56dd80a041ef0e78d59cf8d85997331b245addde27ac495c03`.
- Replayed the remaining checkpoint-7 f222 work into three exact
  checkpoint-8 roots. v418 (v413 row 2 seed503/p0) has two rows, a prefix
  hash of
  `070a550bd6b15514154663d1eab825f2ff77e128f9351bfa6c62fd5616969a60`,
  and an augmented-CNF hash of
  `e84af1ae7f651f68bab137430e5ba53a20436a2bb5a340baab8abfbc9d89273a`.
  v419 (v414 row 1 seed521/p0) has one row, prefix hash
  `ec1ff20b0c14df5bf9c276148df13375875e3406216fede65b8a2efc4c2d25b7`,
  and augmented-CNF hash
  `f3c904e0792d913df1d588b2d205fb1a56aa0b779e1124910416e540f6bab8f0`.
  v420 (v414 row 3 seed541/p0) has two rows, prefix hash
  `35dd1967866b80b44600358fe1a93225fa8e64bf7db93d9b07b2eb9f42fd2aa8`,
  and augmented-CNF hash
  `f06a267d09aab813457f6edb8983ad36d6c87f4944472b3ba69390bc8727adf3`.
  Launched all five child producers on the ARM builder with exact
  nonexisting output targets, the v7 internally framed runner, 14,400-second
  wall checkpoints, and seeds/phases 701/0, 709/1, 719/0, 727/1, and 733/0.
  They were alive with the recorded parameters after launch; no child result
  is claimed yet. With 64 cores, load below 9, 790 GiB free on `/data`, and
  93 GiB free in `/dev/shm`, launched one opposite-phase race per row using
  seed/phase pairs 743/1, 751/0, 757/1, 761/0, and 769/1. All ten jobs use
  distinct nonexisting output targets and were alive after parameter checks.
- Started a separate proof-producing attack on the only three remaining
  computational inputs of the optimized `R(4,5) <= 25` theorem. Copied the
  complete audited 25-branch fixed-star family to durable `/data` storage;
  its manifest SHA-256 is
  `7a88819560bc19bbd4e4696e81b46000949911db8b969c0d272d54fac716533e`.
  The exact d08/d10/d12 input hashes are
  `8c0935f6f795dfb059e6f9c5ba3bd1ae48856e90d3b652b7a81886629b6744db`,
  `974750e1771b1d81687d075faa112f545a7fab34dd4148358ed40755def51339`,
  and
  `f9864879bd1e57f1f0448d89373be24b44cb6228dc299a6ae75c78febc798e7d`.
  Added the exact two-row `x1`/`not x1` root cover (SHA-256
  `8eac79bf2c6cc51e29803f40261ef477d5a7ce1ae6f4000281cce22b15a61f4b`)
  and launched two v7, 14,400-second proof races per degree with seed/phase
  pairs 801/0--809/1, 811/0--821/1, and 823/0--827/1. All six jobs were alive
  with the expected parameters after launch; no UNSAT result is claimed.
- Both v418 row-0 races closed their exact one-root DFS trees. The smaller
  seed743/phase1 trace used 17 attempts and 8 splits and produced a framed
  301,508,712-byte no-empty fragment with SHA-256
  `b188656e5925cca002ae1a675cbcc44cd499e90f9fc5e7e32e74ec58ba6b741b`.
  Its producer log and TSV hashes are
  `53bfc937fdb0ab4162ca284a98fc5ad75db53821d7d30655d3c5549e07b7b080`
  and
  `bba97dff3bee2a9e41de26d0edbf4ee6a5dc11e8fe9846bba6be4760e5ff9129`.
  Independently materialized the exact child CNF (SHA-256
  `84e9deb1777a7d66a8ad83d3c71e9d67de7ac76b73620e5282ccc73e6128abb7`)
  and completed the protected-CNF ordinary checker path. The 300,958,672-byte
  standalone proof hashes to
  `2ea20b6485b562b34108769c9ab62219e2ba48e02cd34a8a1064904f9c35bce9`;
  ordinary `drat-trim` verified it in 701.55 seconds with 537,736 KiB peak
  RSS, and its checker-log SHA-256 is
  `b12136b31e4724469ba430b5aa0fea6fe26a8a10bdb1e70971380653b4cf31a1`.
  The independent source audit passed in 52.47 seconds and hashes to
  `4d92ff1915e2993d50ce944318ddd17e8ce66bc757d5f4daa0be12a53af122ab`.
  Exact final-empty removal produced the 300,958,670-byte embeddable fragment
  with SHA-256
  `8c97f588e42b0ca5243390dbb5e1b833b9d8c7bae9ccdb8f0f6b0efcc3412973`.
  The promotion manifest hashes to
  `79647c1e83a553801c75789e636050778a3c50774740c0d0dbcf87c0f475999b`;
  the final independent audit reran the source audit, passed in 58.10
  seconds, and hashes to
  `508681fcf858bf775029188163b41b0e19e120849d4041ef50f9ee5bfbd9315d`.
  This accepts v418 row 0 as an embeddable child; v418 row 1 remains open.
- Started the same deletion-preserving ordinary-checker alternative for
  f220, f31s1, and f320 while their older addition-only checks continue.
  Added `run_checked_protected_cnf_promotion.sh` to make the full sequence
  reproducible: protected composition, ordinary `drat-trim`, independent
  source audit, exact final-empty removal, and an independent promotion audit
  with a source-audit rerun. The smoke test exposed and fixed a control-
  character edge case in parsing `drat-trim`'s `s VERIFIED` line. The final
  end-to-end ARM smoke passed in 0.77 seconds with 63,488 KiB peak RSS; log
  SHA-256 is
  `f235ed9085c43673ce3db39cbf1487a9bd2d0cff37a288255607ee9b604d6270`.
  All four production pipelines have post-check watchers that require both
  checker exit status 0 and the exact logical verification line before
  starting any audit or promotion. The complete ARM Python suite still passes
  all 232 tests in 26.552 seconds; its new log SHA-256 is
  `430cce551a3c3fe5d00693116afaae233b9c60080810db5c831f7665ccdac393`.
- Added `select_cadical_dfs_race.py`, which replays every completed race,
  checks the exact DFS stack against its final log, validates attempt/split/
  depth counts and terminal binary framing, hashes all inputs, and selects a
  completed proof before any checkpointed alternative. Four focused tests
  cover complete selection, minimum-frontier checkpoint selection, unframed
  rejection, and status/frontier disagreement. On the two real v418 row-0
  closures it selected seed743/phase1 by the recorded minimum-proof-size
  tie-break; the selection-manifest SHA-256 is
  `5ef6567f0edb1494e962e9bb703e77f3eea77c33e98ec2b9ba7109781a81f325`.
  The complete ARM suite now passes all 236 tests in 27.321 seconds; log
  SHA-256 is
  `26134751d4350258ff464450b9d5e7e6bd86ca963c8eebe9982db745c62ea03f`.
- Generalized the race selector from one DFS root to a strictly replayed
  ordered forest (`ramsey55.cadical-dfs-race-selection.v2`). It now verifies
  the producer's `root_index=all` and root count, forbids moving to a later
  root before the current stack closes, includes untouched later roots in a
  checkpoint frontier, and recognizes an early `core=0` row as a global
  closure. Eight focused tests now cover both one- and two-root races. All six
  live d08/d10/d12 snapshots replayed successfully against the real two-root
  `split-var1.icnf`; their second roots were still untouched at the check.
  Replaying the already closed v418 row-0 races under schema v2 again selected
  seed743/phase1; the new manifest SHA-256 is
  `876920d5c33cc04f7b243d90491a0501d4668aeeb71499829ce1a67f31aa1dde`.
  Seven non-mutating watchers will run this exact selection after each active
  f222 or d08/d10/d12 race pair stops. The complete ARM suite passes all 240
  tests in 27.799 seconds with 197,664 KiB peak RSS; the log SHA-256 is
  `a1b3e9003b4959c6b45461e250a1ce6ef7b813147930eef1616d232d40d07f2b`
  and the GNU-time log hashes to
  `d97ad168aa2125bfcc874f46b63f7ad5c0b2abe3191af794bb5d2f136798e938`.
- Upgraded `replay_cadical_dfs_prefix.py` to the matching v2 ordered-forest
  semantics and taught prefix framing, checkpoint finalization, and its
  independent auditor to accept both the historical v1 and new v2 manifests.
  The replay preserves completed earlier roots, the active LIFO frontier, and
  untouched later roots, while rejecting early root transitions and an
  already globally closed prefix. A copied live d12/seed823 snapshot replayed
  73 exact attempts and 45 splits into root-frontier counts `[18, 1]`, showing
  that a four-hour two-root checkpoint can be continued without discarding its
  proof prefix. Three new focused tests cover the two-root frontier, transition
  rejection, and `core=0` closure. The full ARM suite passes all 243 tests in
  28.035 seconds with 197,740 KiB peak RSS; the test-log SHA-256 is
  `3be2c29450c9fb7a45bed51f17c00b2db8934f130e053c2104d1daf6509650fe`
  and the GNU-time log hashes to
  `861c65e47b50c41b1087e2822496368304d73adf72f0b0f8d60b2d8d02012a9e`.
- Added an independently reconstructed Gauthier--Brown generalized-graph
  gluing path for the remaining `R(4,5) <= 25` branches.  The generator
  decodes the leading-one base-three upper triangle and substitutes both
  diagonal blocks into the 24-vertex `R(4,5)` formula; a separate verifier
  rebuilds every clause.  The published cover archive is 80,805,094 bytes and
  hashes to
  `2b3865c813f1568b757b242c62130716f374d6c687c68b798c7b300f34755f30`.
  Its d08 inputs contain 27 `R(3,5;8)` and two `R(4,4;16)` generalized
  graphs, yielding only 54 gluing formulas.  All formulas generated in 5.99
  seconds and independently reconstructed in 4.53 seconds; the exact branch
  manifest SHA-256 is
  `868fbdcc094e14840c9589480cd134b6ae3a08fd2a8933e73bb09b98ef60e60b`.
- CaDiCaL solved 53 of the 54 d08 gluing formulas inside 60 seconds; the last
  solved in 136.04 CPU seconds.  The proof-producing run and ordinary
  `drat-trim` accepted all 54 branches, binding 510,872,280 proof bytes in a
  manifest with SHA-256
  `dd3bb57079f53d5e153c1a6146174364716ea2e7b57d66b05f2a99c4ca23858f`.
  A fresh 12-way replay verified all proofs again in 176.66 wall seconds; its
  audit hashes to
  `8e070a197e5c2676f2b2c2e0e3ffe6382a35481cde83c5c4d7db713a5c26847d`.
  The 54 CNFs, both cover inputs, manifests, and compact logs are retained
  under `data/certificates/r45-gluing-d08`; the proof files remain on the ARM
  host and are individually hash-bound by the committed manifest.
- Added an exact local cover auditor.  It validates every encoded child and
  permutation, enumerates every filling of each generalized graph's at most
  four holes, checks the clique conditions, and uses exact backtracking graph
  isomorphism for completions intentionally covered by an earlier row.  The
  two real audits accepted 27 generalized graphs/179 normalized children and
  two full order-16 graphs; their JSON hashes are
  `0d0eadb8f72ef643fe583438368bf0d36479169b3d7d57e2eb9644ce9fe15aab`
  and
  `c1e17267e8693115a547171521f575355a34e866d12136fe36e4e5266b69c7dd`.
  Global exhaustiveness of those isomorphism classes is still a separate
  enumeration theorem, so d08 is not yet claimed closed.  A fresh upstream
  HOL4 tree fixed at commit `065c07054483e3132f12909103e6d0e35e912c28`
  is being built on the ARM host to replay that boundary.
- Added the formally implied all-vertex degree-window `[7,13]` to the direct
  d08/d10/d12 formulas with independent generator/verifier implementations.
  The three outputs each have 6,180 variables and 88,460 clauses.  Matched
  300-second CaDiCaL/Kissat tests solved neither raw nor strengthened inputs;
  the extra propagation showed no decisive speedup, so gluing remains the
  production route.  After all new tests, the complete ARM suite passes 250
  tests in 31.787 seconds with 197,688 KiB peak RSS; its log and GNU-time
  hashes are
  `953e9d08c6608176339c6e54fcdf3cfb63e38123f74ed2456808c73eeaf77780`
  and
  `37e4a9273dbb87aba05b22b7997aa360c0ad6ab2254b4c41d3285ecc7a27772b`.
- Extended the generalized-graph gluing tools with a sparse branch schema and
  independent reconstruction, then completed a deterministic 128-formula d10
  sample. All 128 CaDiCaL results were accepted by ordinary `drat-trim`. The
  branch/proof/summary manifests hash to
  `4cc5400ac50b48d0e52aa29cb43686692a7b3bba0b76b03b1072b7bfd38ac9b3`,
  `23f7c9ccc1cdcdd3f1ab0cd0ca5e03d55de7eb6b93c55e882a0aeda37991d209`,
  and
  `0a13cf86128e6cd5373b9c171fc3eeda7e05ce14235a4c40dbf48d1c3d236818`.
  The 7,053,352,972 proof bytes have 42,139,126-byte median and
  235,039,916-byte maximum. The 505,336-pair full product is not claimed.
- Built an exact d08 selector CNF with 305 variables and 54,047 clauses and
  independently reconstructed it. The CNF/manifest hashes are
  `ff624fde6a2a767983b9c7accabf05720a64116ed940a5d32f56f68dffc23f0f`
  and
  `27fc655783131319d696954d425b59500e38da1c85372f1504078395f1928f55`.
  After 1,047.81 user seconds it had not solved and its partial trace was
  already 551,006,208 bytes, larger than the complete 54-proof aggregate.
  The deliberately terminated partial trace hashes to
  `6323efa357ecc1cf4b3095a8f1f53c9fb6f1cec4a62867e0604c9cc13cbdfc6c`;
  it has no checker result and proves nothing.
- Diagnosed the pinned old HOL4 tree's external MiniSat 1.14p as unsound on
  ARM root-unit formulas. A two-variable UNSAT CNF was incorrectly reported
  SAT with model `-1 -2`; a one-line simplification patch fixed only the tiny
  case and still failed on HOL-generated formulas. Disabling the external
  executable selected HOL4's internal DPLL route. The clean pinned HOL4 build
  then succeeded, followed by the definition and basic-Ramsey theories. The
  runtime/def/basic logs hash to
  `db7950046dc3465fd90fbcae10ff016b05413068bd1719d1b385e2409339c3ab`,
  `153f741d8b1c08dcbbe4d84ded05aaa5fbe9d950af890b4933c4b21243515ea3`,
  and
  `d566bc975c858cc8e65598f85e01e13045addfda7ca4b1bed00027f44529c88c`.
  The resulting `ramsey_4_5_25_hyp` remains conditional on the three gluing
  obligations and order-24 witness. Generated 1,239 enumeration scripts for
  orders 8 through 17 and started an eight-way durable Holmake replay.
- The pinned upstream `r4524existTheory` then built successfully with the
  external MiniSat still disabled: 758.43 user seconds, 12:46.54 wall,
  7,321,892 KiB peak RSS, exit zero. The generated theory, build log, and time
  log hash to
  `3f7d7ce365563e333aec821dcb708e15af06d85a37dd5b5dd1061ffb7a00b5bf`,
  `05a530da33969996b51ecabb1f5012b6f76d9e0220c9c95fdf75bbff04e25425`,
  and
  `e6ad415b1cfe86556cf24978e3aaa7786c7db41846940382bc56003633b7ee5c`.
  This checks the order-24 lower witness, not any upper-bound gluing case.
- Tested and rejected a 12-worker HOL enumeration setting without losing a
  completed theory. The original 8-way dispatcher was stopped at 98 complete
  five-artifact theories; its boundary evidence hashes to
  `6bc041db2c37bbe0897e99f5c8f37a3cfd5da27d2e5433b1c211f2c95d75ac26`,
  `a70f6414c41f771108fe614126dc63625d158c540f3a68ce140b7cf8c836c159`,
  and
  `3bfca9830809126e333179665d5258fe8b1a0415dbf0669be1d5416448d7b26e`.
  The 12 k=12 workers later reached 124.9 GiB aggregate RSS and left only
  27 GiB available with no swap. The dispatcher was stopped; four largest
  workers were terminated before producing final artifacts, while eight
  finished naturally. Exact `genscriptdep` reconstruction and a five-artifact
  check retained 106 complete theories before resuming at the safe `-j 8`.
  The terminated-worker list and j12 logs hash to
  `064b4064eacb5ce79d81e628c944fabca016a07cabb2bdeffbed70f28c65a336`,
  `78f45cd489123843ffca6b1ade8acea17534dd1dda1a6899f023bda154cd1ddd`,
  and
  `b5b0a18c119861ca85f04f8376022d2a0387e5d7e9f197889298e3518d80fcde`.
- Added a fail-closed enumeration artifact audit and guarded `enumf`
  generator. The audit requires every generated enumeration script to have
  exactly one nonempty five-artifact theory family and hash-binds all inputs;
  final-script generation additionally checks the exact 1,239-theory import
  set. Four focused positive/negative tests pass.
- The f320 protected-CNF promotion completed its full acceptance chain. The
  4,265,217,107-byte standalone proof hashes to
  `c6c9680612b3b2ea4c10aeb158ee7e4381b9daddcb1530c56d9d0390fac49b08`;
  ordinary `drat-trim` emitted exact `s VERIFIED` with exit zero. The
  independently audited 4,265,217,105-byte embeddable fragment hashes to
  `aca69df4aa4601340f0b3b3089624ac750dfee16c7379cd99de873847924fc6d`,
  and the promotion audit hashes to
  `0d302aea8b3d6e5da089fa0364724357ff92a217fcfad6b8f517204f256dc1ae`.
  This closes f320 as a child only; f321 and the enclosing f32 group remain
  open.
- The f220 protected-CNF route also passed ordinary `drat-trim`, an
  independent source-composition audit, exact final-empty removal, and a
  promotion audit that reran the source audit. The 4,572,611,588-byte
  standalone proof hashes to
  `f3341dc7bda40e28a31d81e03db4e84e2487b3981bbdae4d9f3d8e1617f332a7`;
  the 4,572,611,586-byte embeddable fragment hashes to
  `047288cff187b5069eee9c85b74ac8dce6a72ca18f683f364cbda8d886287c95`.
  The source and promotion audits hash to
  `a5d3329a559a4f8ed63ac2d3cd60d08a062243d6eeb222b887692c6b59ffeaa1`
  and
  `723885859438678aab745a5683e26a29b022ab816c506510784742f047a2cba6`.
  This makes f220 and f221 consumable children; f222 and therefore f22 remain
  open.
- After the sparse-family, selector, measurement, and HOL runner changes, the
  complete ARM Python suite passed all 256 tests in 37.698 seconds with
  197,660 KiB peak RSS. The test and GNU-time logs hash to
  `d6f616f8ae2519434a92c08574e922a11c188611cac2723cc09e7f906e662b76`
  and
  `7134993a86c10782159b44338beeec4611686548cea817610bc1c3b1e294e7a4`.
- Added an atomic checked-core compactor for gluing proof families. It reruns
  each source proof with `drat-trim -l ... -C`, requires exact verification,
  then ordinarily checks the emitted core before publishing. All 128 d10
  sample cores passed in 23:57.36 wall. The source total fell from
  7,053,352,972 to 4,591,869,981 bytes; the core manifest hashes to
  `7750e655b042d15acd63fb873131319934614a7f649c6cbdf4fb86fe9887a500`.
  Lossless zstd-1 compression, with every decompressed SHA-256 matched back to
  its core, reduced the sample to 1,513,909,974 bytes. The compressed manifest
  hashes to
  `0ac30aeb681584afef201478064ce74f5cf11fdc8ac4795e6c73b83f724d4808`.
  Mechanical full-d10 scaling is about 5.98 TB retained and 22.85 days on 64
  cores for solve/core/replay, so the current 2 TB disk is not a suitable
  full-family store. This is capacity evidence only, not an unsampled result.
