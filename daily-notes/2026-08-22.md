# 2026-08-22

## Screened frontier continuation

- Recursive extension audits accepted v31 and v32 after two J297 screens on
  `1900`/`2334` and `1210`/`1308`.  The four compact DRATs were 20, 8, 20,
  and 8 bytes.  J297 reached state 616 at two VERIFIED / two UNKNOWN; v32
  chain/parent bundle hashes are `1871a5bb...ff7a` / `afbeb928...1718`.
- Froze and independently replayed a CaDiCaL proof for the already accepted
  J326 state-635 child.  The 176,141,701-byte DRAT has hash
  `719593ac...6e9a`; checkpoint manifest and replay-log hashes are
  `e3b65524...f116` / `ea389d56...45a9`.  This is an independent cross-check
  of the authoritative Kissat proof and does not add a chain segment.
- An expanded 1201--2400 J326 screen selected variable `1933`.  Its 212-byte
  compact DRAT (`5484e90f...e5ed`) advanced J326 to state 637 at one
  VERIFIED / one UNKNOWN.  In parallel J297 reached state 617 on
  `2198`/`2114`.  A deliberately repeated replay of the 179 MB J326 seed
  accepted v33 at 42/38 segments.  Chain/parent JSON hashes are
  `24aecd5f...2fa5` / `a5de46d2...dee6`.
- Two further J297 screens selected `1974`/`1597` and `2103`/`2005`; their
  184/20- and 14/14-byte compact DRATs advanced the local chain to state 619.
  Recursive v33-to-v34 replay accepted both segments.  Chain/parent bundle
  hashes are `09b695ed...7bb7` / `2b344243...6928`; chain and parent audit
  JSON/log hashes are `03a2cc14...53d4` / `9bde0f97...a1d6` and
  `a9478d42...d088` / `efd4e77b...1b29c`.
- The accepted endpoint is now J297/J326 44/38 segments at states 619/637,
  with two/one UNKNOWN descendants.  The next J297 screen, exact J326
  residual portfolios, J297 parent-0 DRAT compaction, and an iGlucose
  cross-check compaction continue on the ARM builder.
- A J297 screen on `1553`/`1641` produced 8/26-byte compact DRATs and advanced
  J297 to state 620.  Recursive v34-to-v35 replay passed; chain/parent bundle
  hashes are `198f19b6...b0ee` / `50c779c1...a4bd`.
- Three J326 screens selected `1704`, `1553`, and `1688`; their 26-, 8-, and
  212-byte proofs advanced the one-cube frontier through state 640.  All
  local and recursive v35-to-v38 chain/parent audits passed.  The final
  chain/parent bundle hashes are `f9e932f9...7da5` / `39b9c101...fc6d`,
  with audit JSON/log hashes `d24197d4...dace` / `6253fb1a...f96c` and
  `4a69cee1...553b` / `af727896...369b`.
- The accepted endpoint is now J297/J326 45/41 segments at states 620/640,
  with two/one UNKNOWN descendants.  The next J326 screen is running.
- The next J326 screen selected `1339`; an 8-byte proof advanced the frontier
  to state 641.  Recursive v38-to-v39 replay passed.  Chain/parent bundle
  hashes are `cf20cfc5...5a19` / `6263f4cf...7d79`; chain and parent audit
  JSON/log hashes are `73ac0680...7ec1` / `d40d484e...8632` and
  `d3348e6d...713b` / `b6e44bfd...0943`.  Accepted endpoint: J297/J326
  45/42 segments, states 620/641, two/one UNKNOWN descendants.
- Three further J326 screens selected `1352`, `1422`, and `2114`; their
  20-, 32-, and 32-byte compact DRATs advanced the one-cube frontier through
  state 644.  Recursive v39-to-v42 replay accepted all three segments.  The
  v42 chain/parent bundle hashes are `dfb400f8...8811` / `353a35b6...43d1`;
  final chain and parent audit JSON/log hashes are
  `f9795caf...dc01` / `7263b6bd...8115` and
  `7f4e77cb...7017` / `6152c19f...2641`.
- Accepted endpoint: J297/J326 45/45 segments, states 620/644, two/one
  UNKNOWN descendants.
- The state-644 J326 screen selected `1309`; its 20-byte compact DRAT advanced
  the frontier to state 645.  Recursive v42-to-v43 replay passed.  Chain and
  parent bundle hashes are `2848f062...17ee` / `db37da4f...ae34`, with audit
  JSON/log hashes `7958e339...52fe` / `e10cec68...edf3` and
  `bddd178a...7df0` / `8b0ff20c...03db`.
- Accepted endpoint: J297/J326 45/46 segments, states 620/645, two/one
  UNKNOWN descendants.
- The next J326 screen selected `1681`; its 56-byte compact DRAT advanced the
  frontier to state 646.  Recursive v43-to-v44 replay passed.  Chain/parent
  bundle hashes are `b5e34828...0f32` / `1ba3bbd3...1dc1`; chain and parent
  audit JSON/log hashes are `42c5400a...026d` / `bc2d1ef1...65db` and
  `0c8b0f38...a829` / `b801d371...1d9d`.
- Accepted endpoint: J297/J326 45/47 segments, states 620/646, two/one
  UNKNOWN descendants.
- The state-646 J326 screen selected `1597`; its 20-byte compact DRAT advanced
  the frontier to state 647.  Recursive v44-to-v45 replay passed.  Chain and
  parent bundle hashes are `31bcfa82...a496` / `c65d3315...6d6d`, with audit
  JSON/log hashes `bb68ff0f...bdf1` / `c8ea99f0...5637` and
  `bd70f26c...fdba` / `fac35b0c...bbbb`.
- Accepted endpoint: J297/J326 45/48 segments, states 620/647, two/one
  UNKNOWN descendants.
- The next J326 screen selected `2101`; its 26-byte compact DRAT advanced the
  frontier to state 648.  Recursive v45-to-v46 replay passed.  Chain/parent
  bundle hashes are `61c340b5...9a5c` / `5e477c32...0668`; chain and parent
  audit JSON/log hashes are `8b76f0ff...3d47` / `9309ccda...2c62` and
  `9d9b7f6d...5e19` / `6c30c586...9b7d`.
- Accepted endpoint: J297/J326 45/49 segments, states 620/648, two/one
  UNKNOWN descendants.
- The state-648 J326 screen selected `2005`; its 14-byte compact DRAT advanced
  the frontier to state 649.  Recursive v46-to-v47 replay passed.  Chain and
  parent bundle hashes are `974e920a...3c9a` / `eee597e9...6e4d`; chain and
  parent audit JSON/log hashes are `7c8c319e...5880` / `410cd62e...e54b` and
  `5520cf11...3c2b` / `48653ab2...1acb`.
- Accepted endpoint: J297/J326 45/50 segments, states 620/649, two/one
  UNKNOWN descendants.
- The next J326 screen selected `1612`; its 20-byte compact DRAT advanced the
  frontier to state 650.  Recursive v47-to-v48 replay passed.  Chain/parent
  bundle hashes are `500cdb24...ce4c` / `91e45aa9...7efc`; chain and parent
  audit JSON/log hashes are `9e418aff...48db` / `d90f6adb...adb1` and
  `6e27ddb1...560f` / `ea62c644...f28f`.
- Accepted endpoint: J297/J326 45/51 segments, states 620/650, two/one
  UNKNOWN descendants.
- The state-650 J326 screen selected `1398`; its 14-byte compact DRAT advanced
  the frontier to state 651.  Recursive v48-to-v49 replay passed.  Chain and
  parent bundle hashes are `799cdda4...1457` / `bd9853df...3c7b`; chain and
  parent audit JSON/log hashes are `aeac43b0...ae85` / `c5c2cf33...2d54` and
  `5f1a0ad8...1a95` / `f58d65db...e03c`.
- Accepted endpoint: J297/J326 45/52 segments, states 620/651, two/one
  UNKNOWN descendants.
- The state-651 J326 screen selected `2238`; its 126-byte compact DRAT advanced
  the frontier to state 652.  Recursive v49-to-v50 replay passed.  Chain and
  parent bundle hashes are `ef78b3b2...2da6` / `92d78656...d2c0`; chain and
  parent audit JSON/log hashes are `fbab3338...2400` / `86da0a8e...a6a9` and
  `388db78c...8dd3` / `211365f9...2870`.
- Accepted endpoint: J297/J326 45/53 segments, states 620/652, two/one
  UNKNOWN descendants.
- Resuming J297, a two-parent screen selected `1270`/`2274`; 14/152-byte
  compact DRATs advanced it to state 621.  Recursive v50-to-v51 replay passed.
  Chain/parent bundle hashes are `6c594181...6847` / `4903cda5...e436`;
  chain and parent audit JSON/log hashes are `1547bebc...56fe` /
  `71f33360...aa28` and `2fc58361...34fd` / `1c9d40bd...4824`.
- Accepted endpoint: J297/J326 46/53 segments, states 621/652, two/one
  UNKNOWN descendants.
- The next J297 screen selected `1259`/`1859`; two 8-byte compact DRATs
  advanced it to state 622.  Recursive v51-to-v52 replay passed.  Chain/parent
  bundle hashes are `47e96e6a...32fc` / `19494234...954b`; chain and parent
  audit JSON/log hashes are `56ca597b...3ce7` / `5195a587...6675` and
  `ebc48e95...d046` / `e9fe6cec...8daa`.
- Accepted endpoint: J297/J326 47/53 segments, states 622/652, two/one
  UNKNOWN descendants.
- A third J297 screen selected `2159`/`1901`; 26/20-byte compact DRATs
  advanced it to state 623.  Recursive v52-to-v53 replay passed.  Chain/parent
  bundle hashes are `21fb0859...8196` / `f8eac5ea...dfb6`; chain and parent
  audit JSON/log hashes are `42c8e781...6e70` / `0afcbddb...5da0` and
  `238e3a68...4d1c` / `6de8c7f5...5220`.
- Accepted endpoint: J297/J326 48/53 segments, states 623/652, two/one
  UNKNOWN descendants.
- The next J297 screen selected `1994`/`1915`; two 8-byte compact DRATs
  advanced it to state 624.  Recursive v53-to-v54 replay passed.  Chain/parent
  bundle hashes are `47a685df...539e` / `c60f63fb...ab2b`; chain and parent
  audit JSON/log hashes are `a84ca078...13c7` / `a8a77dcc...201b` and
  `9c08a1a0...847f` / `4cd6004a...7913`.
- Accepted endpoint: J297/J326 49/53 segments, states 624/652, two/one
  UNKNOWN descendants.
- The following J297 screen selected `1410`/`1899`; its 32-byte/38-byte
  compact DRATs advanced the chain to state 625.  Recursive v54-to-v55
  replay passed.  Chain/parent bundle hashes are `1be98c00...ff39` /
  `3ec100e6...fec`; chain and parent audit JSON/log hashes are
  `57ff0ce4...e471` / `9226b3c3...a96` and `342c8892...ff60` /
  `86ef3107...592`.
- Accepted endpoint: J297/J326 50/53 segments, states 625/652, two/one
  UNKNOWN descendants.
- The next J297 screen selected `2239`/`2304`; two 8-byte compact DRATs
  advanced the chain to state 626.  Recursive v55-to-v56 replay passed.
  Chain/parent bundle hashes are `ca315d02...f2de` / `cd69ab53...27ce`;
  chain and parent audit JSON/log hashes are `2bc40494...d058` /
  `92ead672...f7fb` and `494efcea...08d3` / `a5af361b...2599`.
- Accepted endpoint: J297/J326 51/53 segments, states 626/652, two/one
  UNKNOWN descendants.
- The following J297 screen selected `2332`/`1844`; its 20-byte/8-byte
  compact DRATs advanced the chain to state 627.  Recursive v56-to-v57
  replay passed.  Chain/parent bundle hashes are `b610b530...b5e3` /
  `53bf0e0a...8120`; chain and parent audit JSON/log hashes are
  `8ccf00e3...0aaa` / `856172ac...39b8` and `a2fd0142...c2b9` /
  `f692f4c1...d609`.
- Accepted endpoint: J297/J326 52/53 segments, states 627/652, two/one
  UNKNOWN descendants.
- The exact J297 parent-0 state-623 leaf completed under CaDiCaL.  Its
  489,119,715-byte compact DRAT and complete manifest hashes are
  `685e9722...e818` / `e2bed74b...ad5c`; producer replays of both the 1.62 GB
  source proof and compact proof report `s VERIFIED`.  A fresh independent
  replay passed (`audited 1/1 verified=1`, log `07521dbc...a8c`).
- Strict UNKNOWN-only composition produced a complete two-row state-623
  manifest (`725ce327...becc`) and frozen state (`54ef201d...e137`) using a
  hard link for the 489 MB DRAT.  Its zero-round chain replay is running.
- The current J297 UNKNOWN row 0 uniquely descends from this 717-literal
  ancestor and from a shallower 716-literal state-599 ancestor.  The latter
  has producer-checked CaDiCaL/Kissat proofs; an independent replay of the
  601,296,897-byte CaDiCaL proof is running.
- Added `audit_selective_ancestor_closures.py`, supporting either a complete
  selective subchain or one independently replayed direct proof row.  It
  checks base hashes, exact ordered ancestor prefixes, overlap, and all proof
  bindings before reducing a terminal UNKNOWN count.  ARM regression passes
  all 81 `tests.test_cube_tools` tests.  No selective overlay is accepted yet.
- The composed state-623 parent-0 manifest passed a fresh zero-round whole-chain
  replay with `complete_unsat=true` and `final_unknown=0`.  Audit JSON/log
  hashes are `bb450765...ed09` / `c31ea08f...f75`.
- The seven-segment parent-0 selective chain then passed end-to-end replay.
  Its audit (`c364ad0c...be64`, log `6353f92a...1f0a`) closes J297 v57
  terminal UNKNOWN index 0 and reduces the effective J297 residual from two
  cubes to one.  J326 remains at one.  The independent 601,296,897-byte v110
  CaDiCaL proof also passed separately (`audited 2/2 verified=1`, log
  `ea389d56...45a9`) and is retained as a cross-check rather than the
  authoritative overlay.
- Added `audit_selective_ancestor_closures_extension.py` so a replayed overlay
  can be propagated through a separately audited ordinary chain extension.
  It reconstructs the exact certified ancestor and rechecks ordered prefixes,
  terminal rows, checker identity, and all audit joins.  All 83 ARM cube-tool
  tests pass; implementation commit is `73dfb9e`.
- Three new J297 screens selected `1917/1568`, `2042/1382`, and `1225/1441`.
  Their contradictory-side compact DRAT sizes were `476/28`, `238/20`, and
  `20/448` bytes, advancing the ordinary chain to state 630 with two UNKNOWN
  rows.  Three J326 screens selected `1671`, `2103`, and `1410`; 26-, 14-,
  and 32-byte proofs advanced its unique residual to state 655.
- Recursive v63 replay accepted J297/J326 at 55/56 segments and states
  630/655.  Chain/parent bundle hashes are `ecfd41d5...3ddb` /
  `97ed5589...0416`; the latest one-step chain/parent audit hashes are
  `9588bc41...a90` / `bb12e41f...32a2`.  A direct v57-to-v63 suffix replay
  has hash `43f024c3...e37f`.
- Propagating the accepted ancestor overlay to v63 closes current J297 index 1
  and leaves current index 2 as its sole effective residual.  The propagation
  JSON/log hashes are `911afa6a...6364` / `2faec5be...17a`.  Current effective
  J297/J326 residual counts are therefore one/one.  Four-hour no-proof
  CaDiCaL/Kissat probes are running on the exact remaining J297 state-629 and
  J326 state-655 ancestors in tmpfs; persistent proof generation will be
  attempted only if a probe returns UNSAT.
- Added `audit_selective_residual_chain_bundle.py` to project each effective
  residual exactly, replay its ordinary one-root continuation, and join it
  back to the selective audit.  It rejects source-row, seed, lineage, formula,
  checker, order, and hash mismatches.  ARM regression passes all 85 tests;
  implementation commit is `4a26e0f`.
- The first J297 one-root continuation advanced state 630 to 632 through
  splits `1443`/`1225`; 212-/38-byte compact DRATs closed one sibling per
  round.  Terminal/state hashes are `b5ca189e...12d1` / `20d07933...95a`.
- Projected the unique J326 state-655 residual exactly (root
  `ea3d87de...a6b6`) and created an audited 1 ms UNKNOWN seed
  (`0c783882...63e`).  The first attempted bundle correctly rejected the old
  two-row seed; the second correctly rejected reuse of a frontier manifest
  hash-bound to that seed.  A fresh self-contained one-root workdir selected
  `1842`, closed the negative sibling with a 20-byte compact DRAT, and ended
  at state 656.  Terminal/state hashes are `fe8cfda8...5722` /
  `93129060...cebc`.
- The final two-case residual chain bundle/audit hashes are
  `761bf6bf...3008` / `f6963bc5...581a6c`.  Its selective-residual join passed
  with JSON/log hashes `368f1ff9...c747` / `04028098...33c0`, binding the v63
  selective audit and reporting one J297 plus one J326 UNKNOWN at states
  632/656.  The four long no-proof probes remain live in tmpfs; root disk has
  about 4.8 GB free, so no large persistent proof run was started.
- Continued both joined one-root chains without frontier growth: one
  one-second round reached states 633/657, five 0.25-second rounds reached
  638/662, and ten 0.15-second rounds reached 648/672.  Every binary family
  has one verified compact DRAT and one UNKNOWN; the final ten-round proof
  sizes are 8--332 bytes and all selected contradictions were seen by both
  screen solvers within 0.079 seconds.
- Terminal manifest/state hashes at 648/672 are `84290c9e...2484` /
  `9c6d3e90...f873` and `9a5ae30a...d15a` / `118d68c7...f7d7`.
  Fresh whole-prefix chain replay (`8d39c339...b73e`) checks 18 J297 and 17
  J326 residual rounds.  The v5 selective-residual join audit/log hashes are
  `5eef4ce0...3faf` / `da45bed6...7079`; the effective count remains one/one.
- Twenty more 0.15-second screened rounds per case preserved width one,
  advancing J297/J326 to states 668/692 and total residual-chain lengths
  38/37.  The two new 20-proof batches total only 426/966 bytes.  Terminal
  manifest/state hashes are `882a5dff...6866` / `c7cd8dfc...8412` and
  `4a37e994...98a3` / `5a4aab5b...561e`.
- Whole-prefix replay passed (`5abbb178...7635`, log `e2c0689e...fb9b`), as
  did the v6 selective-residual join (`a80ff39d...b593`, log
  `fd9f55ae...c77c`); both cases still have one effective UNKNOWN.
- Exported the exact deep leaves at states 668/692 to tmpfs (ICNF hashes
  `cea6423b...756c` / `cca3f873...5d24`) and started fresh four-hour
  CaDiCaL/Kissat no-proof probes on both.  They run alongside the shallower
  ancestor probes; a deep-leaf UNSAT result can be joined directly to the
  already audited chains.
- A final 20-round batch per case preserved width one and advanced the
  residual chains to states 688/712, with 58/57 total rounds.  New proof
  bytes total 728/278.  Terminal manifest/state hashes are
  `ee253ce8...c2d0` / `3ad6d531...e828` and `792b2f33...275e` /
  `a938ec7e...6bff`.
- Whole-prefix replay passed (`132c1a69...e162`, log `8b52e9fe...add9`), as
  did the v7 join (`bf022ced...6412`, log `f4b841f3...be08`).  The effective
  count remains one/one.  Paused further screen expansion at about 3.3 GB
  root free; all eight no-proof probes continue in tmpfs.
- Found the builder's separate 2 TB `/data` filesystem (about 1.7 TB free).
  Copied the four v199--v202 residual root/work directories there with hard
  links preserved; checksum dry-runs reported zero changes and identical
  file counts.  Replaced only those inactive root paths with `/data` symlinks
  and removed the verified duplicate copies, recovering about 1 GB.
- Replayed the complete 58/57-round bundle through the symlinks.  JSON/log
  hashes were byte-identical (`132c1a69...e162` / `8b52e9fe...add9`), so
  manifest paths and audit identity are preserved.  Resumed residual-chain
  production with new telemetry writing directly to `/data`.
- Started a 50-round continuation batch through the relocated paths.  Also
  exported exact later leaves from J297 state 697 and J326 state 721 (ICNF
  hashes `878aff96...c4c3` / `ce9946a6...4db5`) and launched iGlucose
  deferred-proof jobs in tmpfs.  They search without proof first; UNSAT would
  trigger a certified rerun, compact DRAT, and checker replay without using
  root disk.
- The 50-round batch completed at states 738/762 with constant width one,
  extending the two residual chains to 108/107 rounds.  The new compact DRATs
  total 1,382/1,938 bytes.  Terminal manifest/state-snapshot hashes are
  `88a2351e...69e6` / `bb4eb819...658d` and `832692d5...135` /
  `5abe2f9a...e87f`.
- Independent whole-prefix replay passed (`d06b7869...210d`, log
  `d5ff13a2...2f3e`).  The v8 selective-residual join passed as well; its
  bundle/audit/log hashes are `df090add...9749` / `6f0f7417...25d1` /
  `a02f9113...6e08`.  Both cases still have one effective UNKNOWN.
- Started the next 50-round continuation from states 738/762.  Telemetry still
  resolves through the repository paths onto `/data`; scratch files stay in
  tmpfs.  Eight CaDiCaL/Kissat solver probes and two iGlucose probes remain
  live and have not yet returned SAT or UNSAT.
- Exported the exact audited state-738/state-762 UNKNOWN leaves to tmpfs; ICNF
  hashes are `8f7af087...d7883` / `971eb11d...dd42`, and their lineage-manifest
  hashes are `dc98f362...fd0d` / `a6166be4...eb5`.  Four new four-hour
  CaDiCaL/Kissat no-proof probes run at `nice=19`, raising the diversified
  long-probe count to 14 without taking priority from the certified chains.
- The next 50-round batch completed at states 788/812, with constant width
  one and total residual-chain lengths 158/157.  Its compact proofs total
  2,728/1,242 bytes.  Terminal manifest/state hashes are
  `c83df79f...e29b3` / `d576534b...7278` and `d039a7e9...3960` /
  `cff99ca8...d137`.
- Whole-prefix replay passed (`d718dc1f...c9b7`, log `fb22625e...f198`),
  followed by the v9 selective-residual join.  Its bundle/audit/log hashes are
  `bd1f2ac5...9d69` / `a861e529...c583` / `0ade4ef0...077d`; effective
  residual counts remain one/one.
- The terminal cubes have 935/933 unassigned variables left in the screened
  1201--2400 range.  Started a third 50-round continuation from the audited
  states 788/812 while all 14 long probes continue.
- The third 50-round batch reached states 838/862 with constant width one,
  extending the residual chains to 208/207 rounds.  New compact DRAT bytes
  total 2,126/1,624.  Terminal manifest/state hashes are
  `3258aefc...0b63` / `63ec07ac...d2a6` and `5979652a...2911` /
  `8ee972f2...4c76`.
- Whole-prefix replay passed (`c4250c8e...b115`, log `810b5c33...1377`),
  followed by the v10 join (`5861c56c...be06` bundle,
  `2d62a609...2cab` audit, `d86543f6...e19e` log).  The effective residual
  count remains one/one.
- The state-838/state-862 terminal cubes retain 885/883 unassigned variables
  in the screen range.  Started the fourth 50-round continuation; all 14 long
  probes remain live and have not returned SAT or UNSAT.
- The fourth 50-round batch completed at states 888/912, growing the residual
  chains to 258/257 rounds without frontier growth.  New compact proof bytes
  total 964/1,306.  Terminal manifest/state hashes are
  `917d0bb6...8432` / `077f2900...6994` and `4fe5a4ff...db7a` /
  `6c9c0b8c...b667`.
- Whole-prefix replay passed (`9085d8b7...b7ee`, log `d25439c4...0600`),
  followed by the v11 join (`71fc6615...c475` bundle,
  `03ea8058...c8bf` audit, `a8bbd903...03e1` log).  Effective residuals
  remain one/one.  Started the fifth 50-round continuation from states
  888/912.
- Two old long tasks published 580/828 MB CaDiCaL/Kissat proofs for the same
  exact v135 state-607 row-1 child-1 cube.  Prefix comparison proves that this
  cube covers the other state-629 UNKNOWN and has one opposite literal versus
  the current effective root, so it is a selective-overlay cross-check rather
  than a new current-residual closure.  Fresh independent replays are running.
- The independent replay of the 827,999,955-byte v135 Kissat proof passed with
  `audited 1/1 verified=1`; its log hash is `07521dbc...ca8c`.  The separate
  579,664,879-byte CaDiCaL replay remains live at low priority.
- Root free space briefly fell to 2.3 GB when those old proofs landed.  The
  inactive 1.4 GB v135 directory and 871 MB completed v144 Kissat subdirectory
  were copied to `/data`, checksum-dry-run verified, exposed at their original
  paths by symlinks, and only then removed from root.  Root free space returned
  to about 4.3 GB; active v144 CaDiCaL state was left untouched.
- The fifth 50-round batch completed at states 938/962 with constant width
  one, taking the residual chains to 308/307 rounds.  New compact proof bytes
  total 1,736/920.  Terminal manifest/state hashes are
  `77f13520...aa88` / `6a1f7e3f...261d` and `014298bf...2901` /
  `6ce8661a...38e3`.
- Whole-prefix replay passed (`ed154ae8...bb92`, log `24ede01d...06a6`),
  followed by the v12 join (`02f52f9b...5b83` bundle,
  `202f72be...36c3` audit, `f87c47b5...f286` log).  Effective residuals
  remain one/one.  Started the sixth 50-round continuation from states
  938/962.
- The 0.15-second dual-solver screen safely exhausted its agreed one-sided
  choices at J297 state 957 and J326 state 984.  The frozen prefixes therefore
  have lengths 327/329; no generated proof or checked round was overwritten.
  The failed-screen parent hashes are `9fdce2bb...823e` / `bee64dd0...5fe`,
  with CaDiCaL UNSAT counts 57/70 and no Kissat answer inside the budget.
- Continued from the exact `r0956`/`r0983` manifests in fresh v203/v204
  segments using a 0.5-second agreement screen.  Both immediately crossed the
  old stop (verified rounds 958/985) and remain width one.  The two-segment v3
  chain bundle has hash `d3b03efc...685d` and records the immutable boundary
  manifests for later whole-chain replay.
- Reused every agreed candidate from those first recovery screens through the
  existing queued refiner: 64 J297 and 58 J326 binary splits were freshly
  proved, compacted, and checked in about six minutes, reaching states
  1021/1042.  Their compact proofs total 87,070/56,624 bytes.  The v4
  whole-chain replay passed (`47b34155...591`, log `4582fdc4...36a9`), as did
  the v13 selective join (`e5276b1d...5de5`, log `ca21dff2...5a5`); effective
  UNKNOWN counts remain one/one.
- Stopped the now-redundant v203/v204 screen process trees at checked states
  979/1006, with no forced kills and all partial-round artifacts retained.
  A screen-only batch at the deeper states supplied another 8/19 agreed
  variables.  Their queued proof chains reached 1029/1061, and v5 whole-chain
  replay passed (`c48163bd...05b`, log `fd480eee...cc1`).
- A one-second screen then supplied 1/2 more agreed variables; all three were
  recertified, reaching runtime states 1030/1063.  A two-second screen-only
  batch is now searching the remaining 693/682 screen variables.  The first
  four four-hour no-proof probes all ended UNKNOWN, while ten later probes
  continue.
- The independent replay of the 579,664,879-byte v135 CaDiCaL proof also
  passed.  Its one-row summary is identical to the earlier Kissat replay, so
  both replay logs have hash `07521dbc...ca8c`.
- The two-second screen supplied another 2/4 forced variables, and a
  five-second plus incremental-screen union supplied 7/6 more.  Recertifying
  every selected sibling advanced J297/J326 to states 1039/1073.  Whole-chain
  v7 and v8 replays passed; the v8 bundle/audit/log hashes are
  `5a31befd...2308` / `d2c2ae35...4945` / `4f1ef871...eacd`.
- Screening the previously untouched auxiliary ranges 2401--5000,
  5001--7500, and 7501--9746 exposed 689/689, 709/709, and 546/539
  dual-solver-agreed candidates.  Added `export_screened_forced_queue.py`
  (`fb1b304`) to bind the exact parent, variable file, screened ICNF, solver
  TSVs, ordering, polarities, and hashes.  All 187 tests passed.
- Added `materialize_forced_queue_cube.py` (`7f3148b`) to compose the predicted
  surviving literals with an exact hash-bound parent while rejecting repeated
  variables or inconsistent polarities.  All 188 tests passed.  The first
  three queues predict exact 3,100/3,139-literal leaves with hashes
  `dd034c89...ef05` / `63408107...8451`.
- All 1,944/1,937 initial auxiliary candidates were then proved, compacted,
  and checked sequentially.  Byte-for-byte comparison showed that the actual
  state-2983/state-3010 UNKNOWN rows equal those predicted leaves.  Recursive
  v11 replay passed with bundle/audit/log hashes `ad495f9f...c938` /
  `5fbbcfd4...cc0f` / `5f1cfb34...7a66`.
- Iterated 0.15-second screens on those stronger leaves.  Candidate counts
  fell 292/314, 10/8, 1/2, 2/0, and 1/0.  The concatenated 306/324 candidates
  were all freshly recertified to states 3289/3334; the actual terminal rows
  again matched the predicted leaves exactly.  v12 replay passed with
  bundle/audit/log hashes `069d2030...0f29` / `b0c93df6...e8e6` /
  `402433a0...2ae9`.
- Raising the screen budget to 0.5 seconds supplied 319/331 candidates, then
  19/17 more.  These were recertified to states 3627/3682 with one UNKNOWN
  each.  v14 replay passed with bundle/audit/log hashes
  `d291e9f7...d48b` / `06a4e255...67a4` / `7fc93e1b...98d7`.
- Two further 0.5-second waves supplied 8/6 and 6/2 candidates.  Every
  contradictory sibling was regenerated with compact DRAT and accepted by
  `drat-trim`; the exact chains reached states 3641/3690.  The actual terminal
  rows again matched their independently materialized predictions, with
  hashes `a1e75f03...6728` / `5b46a8c4...5562`.
- The v16 incremental whole-chain replay passed with bundle/audit/log hashes
  `216b4cf2...6bf7` / `0b98d161...d4da` / `83550c38...db6b`.
  The refreshed selective-residual join also passed with bundle/audit/log
  hashes `9212fd31...294e` / `e98116f2...d7e4` / `0c39b7c3...86e0`.
  It still projects exactly one effective UNKNOWN for J297 and one for J326.
- Started the next J297 screen at a 1.0-second budget on the exact predicted
  state-3641 leaf.  The J326 fifth 0.5-second screen remains in flight; its
  already recertified state-3690 endpoint is the authoritative checkpoint.
- Added an optional staged mode to `refine_screened_binary_cubes.py`: the
  primary solver scans every unassigned variable, while the second solver
  checks only the primary one-sided candidates.  The full primary table,
  projected table, solver bindings, and logs remain hash-bound, and the later
  DRAT recertification is unchanged.  The default parallel mode is unchanged;
  all 190 tests and `ruff check` passed.
- The remaining J326 0.5-second candidate was recertified to state 3691.
  Raising the budget to 1.0 second then produced first waves of 21/22
  dual-solver-agreed candidates.  The staged follow-up waves contained 11/6
  and 5/5.  All 70 siblings were regenerated with compact DRAT and checked,
  advancing J297/J326 to states 3678/3724 without frontier growth.  Actual
  terminal rows match their predicted leaves with hashes
  `f628a769...aae0` / `6ea270f7...5037`.
- The first full-scale staged J297 screen scanned 5,967 variables with Kissat,
  sent only 20 primary candidates to CaDiCaL, and retained 11 agreements.  It
  finished in about 12.5 minutes versus about 27 minutes for the preceding
  parallel full/full 1-second screen while using the same peak screen-job
  count.  A real two-variable ARM smoke test also reproduced the expected
  `[8561, 5807]` queue end to end.
- v20 incremental replay passed with bundle/audit/log hashes
  `175d30fe...ed7c` / `2d25551e...eb4e` / `2059f976...3913`.
  The selective-residual join bundle/audit/log hashes are
  `bf7b13a1...f672` / `a80dffbd...dd8a` / `e9e69edb...a248`.
  The next staged 1-second screens remain in flight on both stronger leaves.
- The next staged waves supplied 3/2 further candidates.  All five
  contradictory siblings were regenerated with compact DRAT and accepted by
  `drat-trim`, advancing the exact J297/J326 endpoints to states 3681/3726.
  The actual terminal rows again match their predicted leaves, with hashes
  `36f38922...8fe1` / `23082e3b...e3dc`.
- v21 incremental replay passed with bundle/audit/log hashes
  `4baf83ca...7ad` / `e430e634...a608` / `c65791ce...b495`.
  The selective-residual join bundle/audit/log hashes are
  `fc1ec6d3...a4a0` / `aee4e294...9d87` / `2e2a2c13...6365` and still
  project exactly one effective UNKNOWN in each fixed-pair case.  The next
  staged 1-second screens are running on those stronger leaves.
- J297 reached a dual-solver 1-second screening fixed point at state 3681.
  Kissat found one one-sided candidate among 5,948 unassigned variables, but
  CaDiCaL did not confirm it at the same budget.  The exact-parent-bound zero
  queue has hash `9aea2557...bbdc`; this is heuristic screening evidence, not
  an UNSAT certificate.  A staged 2-second screen and fresh CaDiCaL/Kissat
  four-hour direct probes now run on the same stronger leaf.
- The parallel J326 screen found eight Kissat candidates among 5,891
  variables, of which both solvers agreed on three: 5232, 5263, and 9121.
  Their queue hash is `532a34ed...a049`.  All three contradictory siblings
  were freshly proved and checked, advancing the exact endpoint to state
  3729; the actual and predicted leaves are byte-identical with hash
  `d58d631f...83af`.
- v22 incremental replay passed with bundle/audit/log hashes
  `d25d24f1...3809` / `7214ed3b...c251` / `db9b03fa...40da`.
  The refreshed selective-residual join hashes are
  `c228f344...e127` / `a2669a8d...a439` / `7c480091...ea31` and still
  project one effective UNKNOWN per case.  The next J326 1-second screen is
  running on state 3729.
- Corrected the newly started v313/v314 storage bindings before substantial
  work accumulated: their first two minutes of files were moved intact to
  `/data` under `*-aborted-root-relocation`, the live paths were replaced by
  `/data` symlinks, and all three J297 jobs were restarted.  No certified
  artifact was removed or affected.
- Relaxed the screening policy without relaxing the proof policy: candidates
  reported by only the primary solver are now attempted because every adopted
  sibling must still carry a DRAT proof accepted by `drat-trim`.  The J297
  primary-only queue (hash `025e8189...6f06`) contained variable 9046; its
  fresh Kissat proof passed and advanced the exact endpoint to state 3682.
  The actual/predicted leaf hash is `dc70151d...1af0`.
- The J326 primary queue (hash `069bc9a6...8530`) contained eight candidates,
  including the three cross-solver agreements.  All eight freshly generated
  Kissat proofs passed, replacing the three-round v22 continuation with a
  clean eight-round segment from state 3726 to 3734.  The terminal row again
  equals the primary-queue prediction, hash `09a9f246...668a`.
- v23 replay from the common v21 base passed with bundle/audit/log hashes
  `0d09ecbd...5baa` / `e59b8115...384f` / `9b4ccaf7...072d`.
  Its selective-residual join hashes are `763c9472...7755` /
  `4e464179...a2cb` / `bdfba54e...6297`; the effective frontier remains 1/1.
  A reverse staged screen now lets CaDiCaL scan all variables on J326 state
  3734 so that CaDiCaL-only candidates are not missed.
- The 2-second J297 screen contained 15 cross-solver agreements but 19 Kissat
  primary candidates.  Its primary queue hash is `de09997d...3158`.  All 19
  fresh Kissat DRAT proofs passed, replacing the one-round v23 J297 suffix
  with a clean state-3681-to-3700 segment.  Actual and predicted terminals
  match with hash `3ba53864...264a8`.
- The reverse J326 screen found eight CaDiCaL candidates and Kissat confirmed
  all eight.  Its primary queue hash is `c9bee68f...2179`.  Composing that
  exact-parent-bound queue with the separately bound 8500 queue produced nine
  proof-verified rounds from state 3734 to 3743.  The terminal row again
  equals the prediction, hash `6abba530...8bcf2`.
- v26 replay, which supersedes the shorter overlapping v23--v25 suffixes,
  passed with bundle/audit/log hashes `d82e0b50...0e26` /
  `aa650880...7529` / `8bb0d494...91e3`.  Its selective-residual join hashes
  are `c7124f0a...b344` / `e2c99a70...c62f` / `954ebc8a...b699`, and the
  effective frontier remains 1/1 at states 3700/3743.
- Stopped the four older non-proof diagnostic probes after the certified
  leaves had strengthened by 19 J297 and roughly fifty J326 literals.  Fresh
  four-hour CaDiCaL/Kissat probes now run on states 3700/3743.  A 2-second
  CaDiCaL-primary reverse screen also runs on J297 state 3700.
- Added `import_materialized_cube_proof.py` so an externally generated tree
  proof can enter the existing manifest/portfolio/chain pipeline only after
  the supplied checker accepts it against the independently rendered target
  augmented cube.  The import binds formula, cube family and index, proof,
  producer/checker binaries, arguments, optional producer log, and every
  artifact hash.  All 191 tests and targeted Ruff checks pass.
- The reverse CaDiCaL-primary J297 screen at state 3700 found variables 2321,
  2576, and 872.  All three fresh CaDiCaL DRAT proofs passed `drat-trim`,
  advancing the exact endpoint to state 3703.  The actual/predicted terminal
  hash is `14a3bc7c...77ea`.  v27 chain bundle/audit/log hashes are
  `ad9e8eb9...265b` / `9a7db118...e75` / `8b267719...6ccd`; its refreshed
  join hashes are `3c4626c9...75a` / `cbb6f467...6eea` /
  `d9659372...0839`.
- The forward Kissat-primary J326 screen at state 3743 found nine candidates:
  9046, 8719, 6888, 8864, 9471, 7860, 9083, 872, and 8925.  All nine fresh
  proofs passed, reaching state 3752.  The actual terminal is byte-identical
  to its prediction, hash `549dda26...679a2`.  v28 chain bundle/audit/log
  hashes are `59e3b959...d39` / `d573aad2...26d` / `1ccd8d6b...502`; the
  join hashes are `0e203973...564` / `7dbf2f85...157` /
  `d1798053...f96`.
- The stale-parent J297 forward screen contributed variable 8719.  It was
  rescreened against the exact state-3703 parent, where Kissat and CaDiCaL
  again agreed, and a newly generated Kissat proof passed `drat-trim`.
  J297 therefore reached state 3704; its actual/predicted terminal hash is
  `d74b5927...5ee`.  v29 chain bundle/audit/log hashes are
  `c1c88cf2...a68d` / `5ac64a0e...017` / `42ae898f...85e`; the join hashes
  are `94e29a90...1794` / `7942da45...a1c9` / `f6a2479c...3fe9`.  The
  effective frontier remains 1/1 at states 3704/3752.
- Proof-producing recursive CaDiCaL trees continue on the weaker state
  3700/3743 leaves.  At the latest checkpoint they had 95/93 split attempts,
  92/91 closed siblings, estimated pending frontiers 4/3, and 871/864 MiB
  proof streams.  Four-hour direct probes also remain in flight.  New
  two-second forward-J297 and reverse-J326 staged screens are running on the
  exact latest leaves.
- Those screens found five J297 Kissat-primary candidates, all five confirmed
  by CaDiCaL, and eight J326 CaDiCaL-primary candidates, one confirmed by
  Kissat.  The primary queue hashes are `a5700081...f94` /
  `fa05e39c...179`; their ordered variables are
  `[6160,7434,5604,6514,4890]` and
  `[5279,8901,7678,1067,8042,5786,9629,8537]`.  All 13 fresh compact DRAT
  proofs passed `drat-trim`.  The exact endpoints reached states 3709/3760,
  and the actual/predicted terminal hashes are `54a50a6c...c99b` /
  `f09f664b...e407`.
- v30 chain bundle/audit/log hashes are `e5835dd5...ae26` /
  `e7ef1e3d...a94b` / `1f59c71f...f97c`; the refreshed selective-residual
  join hashes are `4290d1de...e362` / `1ad60b11...c794` /
  `5088b3e6...3d9c`.  It independently replays 22/23 continuation segments
  and still projects 1/1 effective UNKNOWN.  Reverse-J297 and forward-J326
  two-second screens now run on the latest exact leaves.
- Snapshotted the state-3704/state-3752 high-budget recursive trees while
  stopped, reconstructed their exact live frontiers (four J297 roots and two
  J326 roots), then resumed the source producers and launched each frontier
  row as an independent proof fragment.  The independently materialized root
  augmented-CNF hashes are `ec1adf42...70d2e` / `dda5442b...8d4b`.
  J297 fragment 0 already completed and its standalone composition passed
  `drat-trim`; the root proofs remain incomplete until every fragment closes
  and the final compositions verify.
- The state-3709 reverse J297 screen produced the proof queue `[8901,7980]`;
  both variables had cross-solver agreement.  The state-3760 forward J326
  screen produced primary-only queue `[6706]`.  Fresh primary-solver compact
  DRAT proofs for all three contradictory siblings passed `drat-trim`, moving
  the exact endpoints to states 3711/3761.  Actual and independently predicted
  terminal hashes match at `c6ce9591...4ae1a` / `c5ec9aa0...e008f`.
- The v30-to-v31 extension audit passed.  Chain bundle / extension-audit / log
  hashes are `a98cd644...95ee` / `5601fd9c...3a8b` /
  `aae3563a...abe`; the refreshed selective-residual join hashes are
  `4029be4a...8506` / `8b65919c...e171` / `c1754516...c37d`.  It now binds
  23/24 continuation segments and still projects 1/1 effective UNKNOWN at
  states 3711/3761.  The subsequent full v31 replay passed with audit/log
  hashes `96a9dcee...a3b6` / `a3d2b19d...743f`; its full-audit-bound join
  bundle/audit/log hashes are `bdff1daa...e3c4f` / `51b94847...fc899` /
  `9bf7106a...3c857`.  The next forward-J297/reverse-J326 screens continue on
  ARM.
- The next exact-parent screens each found variable 8743.  Both solvers
  agreed for J297; J326 retained CaDiCaL-primary-only screening provenance.
  Fresh Kissat/CaDiCaL compact proofs passed `drat-trim`, advancing the exact
  endpoints to states 3712/3762.  Actual/predicted terminal hashes match at
  `609e64ce...ba22` / `085f4862...b0319`.
- v32 chain bundle / v31-to-v32 extension-audit / log hashes are
  `d2425a54...61c8d` / `c53b8dbd...e264f` / `008e969b...f87a0`; the
  selective-residual join hashes are `05ce1d90...f388e` /
  `acdc6360...8077c` / `b56fb8f7...f25b4`.  It binds 24/25 continuation
  segments and still projects 1/1 effective UNKNOWN.
- Added a second safe checkpoint layer to the three unfinished proof
  fragments.  After a roughly five-second stop/copy/resume window, exact DFS
  replay reconstructed two plus four J297 subroots and three J326 subroots.
  All nine independent high-budget subfragment jobs run alongside the
  uninterrupted original producers.  Their results remain telemetry until
  prefix-plus-subfragment compositions pass `drat-trim`.
- A resource audit stopped three dominated low-budget fragment hedges, four
  non-proof probes on weaker leaves, and two weaker recursive trees.  Their
  incomplete artifacts remain available; the stronger proof-producing trees,
  Kissat jobs, original fragments, and nine second-level fragments continue.
- The state-3712 J297 reverse screen produced primary queue
  `[4400,5968,9276]`, with cross-solver agreement on 5968 and 9276.  The
  state-3762 J326 forward screen produced `[5978,5604]`, with agreement on
  5978.  All five fresh compact proofs passed `drat-trim`, advancing the exact
  endpoints to states 3715/3764.  Actual/predicted terminal hashes match at
  `606eb8de...410e` / `f812c640...0988`.
- v33 chain bundle / v32-to-v33 extension-audit / log hashes are
  `268495ed...4def` / `d9b069c6...d8cc` / `4035276a...4cb7`; refreshed
  selective-residual join hashes are `902e28c4...a26d` /
  `69b3b82f...bf2d` / `339d072a...419e`.  It binds 25/26 continuation
  segments and still projects 1/1 effective UNKNOWN.  New forward-J297 and
  reverse-J326 screens run on the exact latest leaves.
- Took a later safe checkpoint of the two strong state-3704/state-3752
  CaDiCaL trees.  The J297 prefix/frontier hashes are `1a95c662...271` /
  `433679b7...fdd`; the J326 hashes are `0eba17a5...d74` /
  `5e276bf5...39d`.  Independently materialized root CNFs again match the
  earlier `ec1adf42...70d2e` / `dda5442b...8d4b` bindings.  Nine explicit
  high-budget fragments were launched.  A standalone J326 fragment replay
  already passed `drat-trim`; at this checkpoint 2/4 J297 and 2/5 J326
  fragments are complete, so neither root proof is yet complete.
- The state-3715 forward Kissat-primary two-second screen found no one-sided
  candidate among 5,914 unassigned variables.  Its exact-parent-bound zero
  queue has hash `1f01d151...f6fe`, and its materialized terminal remains
  byte-identical to state 3715 (`606eb8de...410e`).  This is a screening fixed
  point, not an UNSAT certificate; a reverse CaDiCaL-primary screen now runs
  on the same exact leaf.
- The state-3764 reverse CaDiCaL-primary screen found queue
  `[2576,4559,1586,1298]`; the cross-solver agreement queue was empty.  The
  agreement/primary queue hashes are `19b462fc...8422` /
  `0a3b25ef...ea4`.  Fresh compact CaDiCaL proofs for all four contradictory
  siblings passed `drat-trim`, advancing the exact J326 endpoint to state
  3768.  Its actual and independently predicted terminal rows are identical,
  hash `2c1af809...cf0`.
- v34 chain bundle / v33-to-v34 extension-audit / log hashes are
  `72d2d1e7...1af7` / `4a1db9ea...533` / `07a863d9...da6`; refreshed
  selective-residual join hashes are `c350150e...24d` /
  `5e5594b2...1a48` / `0d86e2ba...654`.  It binds 25/27 continuation
  segments, ends at states 3715/3768, and still projects 1/1 effective
  UNKNOWN.  A forward Kissat-primary screen now runs on J326 state 3768.
- The complementary J297 reverse screen found CaDiCaL-primary-only candidate
  4559.  The empty agreement / primary queue hashes are `a31e906a...e69a` /
  `8ba64d2b...0bfa`.  Its fresh proof passed `drat-trim`, reaching state 3716;
  actual and predicted terminal rows match at `6c773ac1...71acd`.
- v35 chain bundle / v34-to-v35 extension-audit / log hashes are
  `4eb59f6b...4820` / `15035e11...5d39` / `263ece9a...f000`; refreshed
  join hashes are `21a26b9e...d65d` / `3fb3a8f3...9616` /
  `ce438b0a...2f4b`.
- The J326 state-3768 forward Kissat-primary screen produced queue
  `[6332,8561,4993]`, with cross-solver agreement on 8561 and 4993.  The
  agreement/primary hashes are `f3d58f90...3e4d` / `57a5b022...3c64`.
  All three fresh proofs passed, reaching state 3771; actual and predicted
  terminals match at `609bd2e9...5e6a`.
- v36 chain bundle / v35-to-v36 extension-audit / log hashes are
  `ca318941...4ef3` / `64f27308...13b9` / `c118735c...746f`; refreshed
  join hashes are `9132b670...618f` / `6e9ede98...d322` /
  `da8ccb32...df69`.  It binds 26/28 continuation segments and still projects
  1/1 effective UNKNOWN at states 3716/3771.
- Added a third proof-parallelism layer after the J297 screen released 16
  cores.  Five late-checkpoint fragments were each stopped only for their
  own three-to-four-second copy window and immediately resumed.  Exact replay
  exposed 3+4 J297 and 1+2+3 J326 subroots; all 13 received high-budget
  explicit-stack producers.  The original J326 fragment 2 then completed, so
  its redundant child was safely stopped with partial artifacts retained.
  Two J297 subfragments have completed; no nested parent-fragment or root
  composition is accepted until its standalone `drat-trim` replay passes.
- The J297 state-3716 forward Kissat-primary screen found variable 5139, with
  CaDiCaL agreement on the same contradictory side.  Agreement/primary queue
  hashes are `81a956db...4acf` / `e8e24ad0...2e8d9`.  A fresh compact Kissat
  proof passed `drat-trim`, advancing the exact J297 endpoint to state 3717.
  Its manifest/state hashes are `1dfcc28d...deeb` / `d405091f...bc4`, and the
  actual terminal is byte-identical to the independent prediction at
  `343020bf...59ff`.
- The v37 chain bundle / v36-to-v37 extension-audit / log hashes are
  `07eb96ef...c233` / `2dad3dfe...e181` / `0b76f95a...b009`; refreshed
  selective-residual join hashes are `0096a501...6d25` /
  `0354c41d...6e3e` / `2e75bc4f...b89f`.  It binds 27/28 continuation
  segments and still projects 1/1 effective UNKNOWN at states 3717/3771.  A
  reverse CaDiCaL-primary screen now runs on the exact J326 state-3771 leaf.
- A third strict subfragment, J326 f3-sub-0, completed with `status=20`.
  The nested J326 f3 group still awaits f3-sub-1 before composition and
  standalone replay, so this completion has not yet been promoted into a
  parent-fragment or root certificate.
- The complementary J326 state-3771 CaDiCaL-primary and Kissat-primary scans
  both completed every 11,692 polarity cube without a one-sided candidate.
  Their exact-parent-bound zero queues hash to `1f7d83b6...d237` and
  `12692e6e...1773`; both materialized terminals remain byte-identical at
  `609bd2e9...5e6a`.  J297 state 3717 likewise reached a CaDiCaL-primary zero
  queue, hash `5e1181aa...b0c9`, with unchanged terminal
  `343020bf...59ff`.  These are two-second screening fixed points, not UNSAT
  certificates; a forward Kissat-primary J297 scan continues.
- The two 14,400-second direct Kissat root attempts ended UNKNOWN with no SAT
  result and no UNSAT proof.  Their partial manifests hash to
  `dcdf1a1e...2f8a` / `0ae35cff...c03b`; independent partial audits both
  report one UNKNOWN and hash to `3d9c660d...6508`.  The matching whole-root
  CaDiCaL trees also exhausted their global budgets with `runner.exit=3`.
  Their later v372/v373 checkpoints and descendants retain the usable proof
  prefixes.
- Added `replay_cadical_dfs_prefix.py`, a single-root, hash-bound replay tool
  for the explicit-stack prover's TSV prefix.  All 194 Python tests pass with
  `PYTHONPATH=src:tools`.  Real regression reconstructed the existing J297
  four-row and J326 two-row frontiers byte-for-byte at hashes
  `f5fcd868...18b53` / `b82c235d...25812`; the tool is public in commit
  `e94301b`.
- Used that replay to add a fourth proof-parallelism layer beneath six
  remaining third-layer roots.  The sources were individually paused for
  only 1.8--2.3 seconds and immediately resumed.  The J326 f3-sub-1 frontier
  has two rows (`98139083...326`); J297 f3-sub-2/sub-3 have four each
  (`bb18637b...9e01` / `20d564c7...39f5`); J297 f2-sub-1/sub-2 have two each
  (`149a85fa...b21a` / `5805cd0a...f2ab`); and the three J326 f4 descendants
  have 2/3/4 rows (`bd80a65b...3327` / `56d04c87...60c0` /
  `6996b2f6...75e8`).  All 23 open roots received independent high-budget
  fragment producers; two had already closed at the first post-launch audit.

No parent-1 UNSAT, fixed-pair UNSAT, order-45 UNSAT, or `R(5,5) <= 45`
theorem is claimed.
