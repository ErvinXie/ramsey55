# 2026-08-21

## Post-v4 narrow-frontier rescues

- Audited explicit growth-adoption snapshots for the two post-v4 chains.
  J297775 replays rounds 243--244 as three proof manifests and four parent
  refinements, ending at 1 VERIFIED/3 UNKNOWN in state round 245; its audit
  JSON/log hashes are `38da332a...6c25` / `23e05e05...d02e`. J326185 replays
  rounds 279--351 as 74 manifests and 73 refinements, ending at 0 VERIFIED/2
  UNKNOWN in state round 352; its hashes are `37ef1def...5c08` /
  `5739210d...ab88`. Both completion claims remain false.
- Kissat solved the first of the two jointly hard J297775 round-244 children
  in 1,985.07 seconds. Its independently replayed compact DRAT is 344,533,164
  bytes with SHA-256 `a80a1e60...493c`; the sibling timed out at 3,600
  seconds. Composition with the exact four-row candidate restored the
  authoritative boundary to 2 VERIFIED/2 UNKNOWN. After relocation v15, the
  source and composed manifest hashes are `7e41e32c...c57d` and
  `eade331b...ac28`; the relocation record hash is `aa66ac0e...2e00`.
  A fresh stable-path replay accepted both retained proofs, with log hash
  `f57494e8...d606`.
- CaDiCaL independently solved the same J297775 child and produced a smaller
  299,667,655-byte compact proof. Its stable-path replay now passes with log
  hash `ea389d56...45a9`; the relocated manifest hash is
  `6729d76d...36d6`. A guarded continuation from the checked Kissat 2/2
  boundary has started at round 245 on the ARM builder.
- Solver-diverse 3,600-second runs on the J326185 round-351 pair have also
  solved the same first child under multiple solvers. Their proof compaction,
  checking, and the still-running sibling attempts have not yet produced a
  finalized manifest.
- The J326185 round-351 rescue is now stable. CaDiCaL and Kissat independently
  closed the same child in 1,335.36 and 1,387.54 seconds; their compact DRAT
  hashes are `ca87f0f0...e96` and `da3953b4...f1e7`. The smaller CaDiCaL proof
  was composed into the exact two-row growth candidate, yielding 1 VERIFIED /
  1 UNKNOWN. After relocation v16, the CaDiCaL source, Kissat cross-check, and
  composed manifest hashes are `fd71c1aa...f88`, `977c048e...16a0`, and
  `6129a9b3...f999`; a fresh stable-path replay passed with log hash
  `ea389d56...45a9`. The continuation kept width one through state round 356
  before round 356 grew `1 -> 2`. Relocation v19 has hash
  `4ddc029b...ffd5`; replay of the adopted growth accepted six manifests and
  five refinements, with audit JSON/log hashes `74ccbf9b...8de3` /
  `1949a368...8b9`. Long solver-diverse retries are running on the exact pair.
- The rescued J297775 continuation completed rounds 245--273 at constant
  width two. Its round-274 candidate has 1 VERIFIED / 3 UNKNOWN and was
  correctly rejected as a `2 -> 3` growth. An immutable state-round-274
  checkpoint has hash `71c615ef...252c`; relocation v18 has hash
  `a903cbb6...c70a`. Three solvers plus seed-diverse CaDiCaL/Kissat runs are
  producing 3,600-second certificates on the exact two-row residual that can
  restore the candidate to width two if either row closes. Stable replay
  accepted 30 manifests and 58 refinements, ending at 2 VERIFIED / 2 UNKNOWN;
  its JSON/log hashes are `8469c9e4...d639` / `be5d8549...f6bd`. One
  relative-path invocation was retained as a failed spelling-only attempt.
- Default CaDiCaL and Kissat independently closed the same first residual of
  the J297775 round-274 candidate. Their compact DRATs are 126,165,358 and
  135,835,133 bytes, with hashes `8dbc90f2...8cae` and
  `0633299e...fda7`. `finalize_materialized_progress.py` freezes completed
  live rows, including atomically published proof/checker artifacts that
  precede a final progress-file update; unfinished rows remain explicit
  UNKNOWN. After relocations v20/v21, the CaDiCaL source, composed boundary,
  and Kissat cross-check manifest hashes are `c1f2b709...33d2`,
  `097d507c...c2db`, and `a4f9094f...6127`. Independent stable replays passed.
  At round 285 the normal CaDiCaL pass proved one child and the 600-second
  Kissat fallback proved the needed child of the other parent; their
  13,441,164- and 23,377,701-byte proofs have hashes `599e35f2...f921` and
  `0638db0d...8087`. The resulting manifest is 2 VERIFIED / 2 UNKNOWN, and
  the continuation has retained width two through state round 301.
- A complete five-second screen of 365 unassigned auxiliary variables on the
  J326185 round-356 parent found 152 one-sided contradictions agreed by both
  CaDiCaL and Kissat, no two-sided contradiction, and one solver disagreement.
  Selecting variable 1092 gave a certified 0.052-second proof for its positive
  branch while the negative branch remained UNKNOWN, preserving frontier
  width one instead of adopting the automatic variable-893 `1 -> 2` split.
  The selected split and two further screened splits on variables 1183 and
  1184 keep frontier width one through state round 359. After relocation v23,
  their stable manifest hashes are `95943232...e69b`, `d419cbe1...2bde`, and
  `a68001a1...cc74`; all three stable-path chain replays passed. Relocation v23
  has hash `97dad516...9b4e`. Three more selected rounds on variables 1199,
  933, and 1190 reach stable state round 362. Their proof budgets were reduced
  from 120 to five seconds; all three still produced independently accepted
  compact DRATs while retaining the opposite child as explicit UNKNOWN.
  Relocation v24 has hash `5f9d66a7...c7a1`; the three new stable manifest
  hashes are `aec42b83...81c9`, `935408c9...b165`, and
  `60836973...5b67`, and all stable-path chain replays passed.
- `select_screened_binary_splits.py` now binds the parents, complete signed
  screen, variable list, and two or more solver-result tables; it rejects SAT,
  solver disagreement, layout mismatch, and missing per-parent candidates,
  then deterministically chooses the fastest agreed one-sided split. Input
  paths are canonicalized before recording so stable relocation cannot depend
  on a lexical `..` component.
- `refine_screened_binary_cubes.py` implements the same policy behind the
  existing materialized-chain refiner interface; the chain runner now forwards
  repeatable `--refiner-argument` values. A production run with 1-second,
  16-way CaDiCaL/Kissat screens and five-second proof mode automatically
  completed rounds 362--364 on variables 1181, 868, and 1149, preserving the
  single UNKNOWN parent. Relocation v25 has hash `f585e0e2...302c`.
  Stable replay accepted all four manifests and three refinements through state
  round 365; its JSON/log hashes are `facc89a5...f2fd` /
  `44104ef4...5a74`. The live automated chain has already reached state round
  366 and continues. The local and ARM suites pass 156 tests.
- Certified iGlucose supplied a third solver-diverse proof for the rescued
  J326185 round-351 child: 354,042,170 bytes, hash `4b50ae1c...2df`. Its
  relocated manifest hash is `49c957e1...8902`; a fresh stable-path replay
  passed with log hash `ea389d56...45a9`.

No parent-1 UNSAT, fixed-pair UNSAT, order-45 UNSAT, or `R(5,5) <= 45`
theorem is claimed.

## Rescued-v5 audit correction and J297 round 331

- Froze rescued-v5 with 16 J297775 segments through stable state round 317
  and 19 J326185 segments through round 365.  The terminal frontiers remain
  two/one UNKNOWN, and the chain/reduction bundle hashes are
  `202485bd...b7c` / `8b3f6830...a21`.  Both claims remain explicitly
  partial.
- Corrected the semantic treatment of the J297 round-242 and J326 round-278
  rescue boundaries.  They are not same-round exact-cube retries: each skips
  one locally materialized complete binary refinement whose proof manifest was
  retried elsewhere.  The auditor now independently reconstructs the prior
  UNKNOWN frontier, checks that refinement, and binds the next seed to its
  exact child hash/count and formula.  It also accepts relocated cube paths
  only when content hash and count agree.  Wrong rescued-child counts are
  covered by a negative regression test.
- The repaired two-layer v5 replay passed.  The chain audit covers 332/383
  proof manifests and 316/364 recorded rounds for J297/J326, reports
  1,234/991 refined parents inside the segments, and separately reconstructs
  the omitted round-242/278 refinements of two/one parents.  Its JSON/log
  hashes are `4b41a35e...5419` / `b954a36c...b29f`; both cases remain partial.
  The parent audit also replays all 193/178 backbone proofs, checks the 15
  certified backbones and their bad branches for each case, and leaves
  `parent_unsat=false` with two/one UNKNOWN descendants.  Its JSON/log hashes
  are `49790b42...cf23` / `682e319a...aa4a`, and
  `all_parents_unsat=false`.
- Created isolated post-v5 snapshot
  `v6-j297-r0331-screened-snapshot`.  The normal round-317--331 stage replays
  15 manifests / 14 rounds / 28 parent refinements and ends 2 VERIFIED / 2
  UNKNOWN; audit JSON/log/final-manifest hashes are `68112948...e751`,
  `90e7f3bf...102c`, and `57ee5a5a...f06a`.  Relocation v27 is
  `fe5c12f3...6963`.
- Bypassed the rejected automatic round-331 `2 -> 3` growth with complete
  CaDiCaL/Kissat screens.  Parent variables 897 and 1179 have checked
  contradictory negative branches; their 14/38-byte DRAT hashes are
  `98f89236...3a2` and `5ccee31c...1b9`.  The one-round audit again ends at
  width two, with JSON/log/final-manifest hashes `0eb2f8c4...0c9`,
  `2f11029d...9fd6`, and `17edd156...3792`.  The live screened continuation
  has progressed beyond this frozen state.
- Independently, CaDiCaL and Kissat both closed child 2 of the exact automatic
  round-331 four-row candidate.  Frozen manifests are `ba98f100...c28` and
  `7db5c555...f4b0`; the three-source composed manifest is
  `a6ab8b14...ca8e` and restores 2 VERIFIED / 2 UNKNOWN using the smaller
  197,471,174-byte CaDiCaL proof.  Fresh replay passed with audit JSON/log
  hashes `a4b3f7fc...6d2c` / `f57494e8...d606`.  Relocation v28 is
  `3536c420...f55e`; its stable CaDiCaL/Kissat/composition manifest hashes are
  `15458de0...ada3`, `45a23301...54b1`, and `9e98c391...a55e`.  Both selected
  CaDiCaL and cross-check Kissat proofs passed again from stable paths.  The
  v27-to-v28 boundary independently reconstructs round 331 and binds the next
  seed as its exact children at first round 332.  The remaining child 3 was
  not closed by independent 3,600-second CaDiCaL, Kissat, or targeted
  iGlucose attempts, so this portfolio did not improve the frontier to width
  one.
- Added `tools/select_cube_rows.py`, which atomically emits selected ICNF rows
  plus a manifest binding source/output hashes, counts, and original indices.
  Redundant CaDiCaL/Kissat targeted duplicates were stopped after their
  original four-row jobs reached the same child; the targeted iGlucose hedge
  subsequently completed UNKNOWN.  The ARM suite passes all 158 tests.  The
  builder reports 32 physical / 64 logical CPUs, 244 GiB RAM, about 19 GiB
  root free, and about 98 GiB `/dev/shm` free.

No parent-1 UNSAT, fixed-pair UNSAT, order-45 UNSAT, or `R(5,5) <= 45`
theorem is claimed.

## Screened continuation checkpoints after rescued-v5

- Froze both live screened continuations at J297/J326 state rounds 378/411 in
  `v8-screened-continuations-snapshot`.  Relocation v29 has hash
  `d05e5510...785b`.  Stable-path replay covered 47/49 rounds, 48/50 proof
  manifests, and 94/49 parent refinements, retaining two/one UNKNOWN cubes.
  The J297 JSON/log hashes are `786cf7c0...c403` / `b96b31f2...ed72`; the J326
  hashes are `c3a3e02a...3a93` / `a5089edb...636c`.
- Created rescued-v6 through state rounds 378/411.  The chain/reduction bundle
  hashes are `afbd6fd0...9e86` / `f93b22a1...719b`.  A complete replay with
  four segment workers and four proof workers per segment passed: J297 covered
  17 segments, 394 manifests, 377 rounds, and 1,356 parent refinements, ending
  2 VERIFIED / 2 UNKNOWN at round 378; J326 covered 19 segments, 429 manifests,
  410 rounds, and 1,037
  refinements, ending 1 VERIFIED / 1 UNKNOWN at round 411.  The audit JSON/log
  hashes are `02cc9059...e00` / `0b90fc7f...74b`.  The first full run
  replayed the earlier J297 segments, then exposed a symlink-path bug at the
  v8/r331 boundary: `.resolve()` changed the certificate-visible manifest
  spelling even though the bytes matched.  The auditor now makes paths
  absolute without expanding symlinks, a regression test covers the case,
  and the full replay was restarted successfully.  The auditor also gained an
  explicit `--segment-jobs` layer: independent immutable chain segments replay in
  parallel, while adjacency is still validated in order afterward.  The
  successful replay used four segment workers and four proof jobs per segment.
  Failed/intermediate audit logs were retained.
- The rescued-v6 strengthened-parent audit also passed.  It replays the same
  complete chain and independently accepts all 193 J297 and 178 J326 backbone
  proofs.  The cases still retain two/one UNKNOWN cubes, so
  `all_parents_unsat` is false.  Its JSON/log hashes are
  `55b8809e...ffff` / `f8dbf65b...8acc`.
- Froze the next J326 checkpoint at state round 462.  Relocation v30 is
  `569028cd...b762`; a fresh replay checked 100 rounds, 101 proof manifests,
  and 100 refinements, still ending 1 VERIFIED / 1 UNKNOWN.  Its JSON/log
  hashes are `d4bc1d06...f0496` / `50eb3bef...5b18`.  The screened chain was
  resumed in place for another 100 rounds.
- Exported hash-bound direct-solver frontiers at J297 round 378 and J326 rounds
  411/462.  Their ICNF hashes are `8774bcb8...c297`, `7c459211...5439`, and
  `d0046a4e...1d94`.  The two-parent J297 round-378 and one-parent J326
  round-411 iGlucose runs returned only UNKNOWN after 3,600 seconds, with
  manifest hashes `4caf1d73...0d8` / `2ec9b687...48b8`.  CaDiCaL/Kissat
  also returned UNKNOWN at round 411, with manifest hashes
  `b4f30fb6...4c13` / `ce2c7857...3bf8`.  The three-solver round-462
  portfolio also returned only UNKNOWN after 3,600 seconds.  Its results were
  relocated and re-audited under v11; relocation v34 is `653b6dfb...6721`,
  stable iGlucose/CaDiCaL/Kissat manifest hashes are
  `6ccc32a9...375a`, `a955c8d3...650a`, and `8fe6e2e0...bd8c`.
- The resumed J326 screened chain reached state 506, then found no agreed
  one-sided split in variables 800--1200 at the one-second screen budget.
  State 506 and the failed screen were preserved in
  `v13-j326-r0506-screened-snapshot`; relocation v32 has hash
  `9b47a38b...6b87`.  A stable replay checked 144 rounds, 145 proof manifests,
  and 144 refinements, still ending 1 VERIFIED / 1 UNKNOWN; its JSON/log
  hashes are `9ab68784...436f` / `62f24443...5278`.  The failed live artifacts
  were moved, not deleted.  Retrying over variables 1--1200 accepted round
  506 and advanced the live chain through at least state 512.
  The state-506 UNKNOWN cube was exported with ICNF/lineage hashes
  `a1786afa...bcb5` / `1f749324...4ae`.  iGlucose, CaDiCaL, and Kissat each
  ran for 3,600 seconds and returned UNKNOWN.  Their stable manifest hashes
  are `e6f5361a...cb2`, `4155a0b6...b67`, and `e05e772f...5e58` after
  relocation v35 (`581fcd4d...030c`); all three stable audit logs report
  0 VERIFIED / 1 UNKNOWN and have hash `3d9c660d...6508`.
- The original four-row J297 round-331 iGlucose run independently verified
  child 2 in 2,296.95 seconds.  Its 438,113,063-byte compact DRAT has hash
  `2eaecb24...ee0e` and source checker-log hash `9f134488...47cb`.  Progress
  was finalized with UNKNOWN placeholders and relocated to v12.  The stable
  replay passed with 2 VERIFIED / 2 UNKNOWN; relocation, manifest, and audit
  hashes are `53a85260...5fb3`, `b7efce2d...5e7a`, and
  `f57494e8...d606`.
- The J297 continuation stopped at state 414 after variables 800--1200 gave
  no agreed one-sided split for parent 1.  State 414 and the failed screen
  were preserved in `v14-j297-r0414-screened-snapshot`; relocation v33 has
  hash `f1a2acdf...a0f9`.  A stable replay checked 83 rounds, 84 proof
  manifests, and 166 parent refinements, ending 2 VERIFIED / 2 UNKNOWN; its
  JSON/log hashes are `ac87ffc6...09d` / `7e6f69ae...802b`.  The failed live
  artifacts were moved, not deleted, and the live chain was restarted from
  state 414 over variables 1--1200; it accepted the failed round and reached
  at least state 415.  The two state-414 UNKNOWN cubes were exported with
  ICNF/lineage hashes `7def28ff...3ac` / `1a7e31f8...aaa`; a three-solver
  3,600-second portfolio, with two solver jobs per engine, returned UNKNOWN
  on all six attempts.  After relocation v36 (`5a2aff93...2664`), the stable
  iGlucose/CaDiCaL/Kissat manifest hashes are `5dacddd0...09a5`,
  `7e4626ce...d69d`, and `b7c6afe7...f391`; each audit reports
  0 VERIFIED / 2 UNKNOWN and has log hash `3f8f88e0...961b`.
- The ARM regression suite passes all 166 tests in 33.949 seconds with
  `PYTHONPATH=src:tools`.  The README test command was corrected to include
  `tools/`; using only `PYTHONPATH=src` fails while importing the screened
  refiner and is not a code-test failure.
- Exported the deeper live frontiers at J326 state 522 and J297 state 418.
  Their ICNF/original-lineage hashes are `d99ee498...3efb` /
  `08d187f1...f3ef` and `bebfc088...d7ec` / `4d583932...6b22`.  The exact
  source workdirs and seed boundaries were copied to stable storage.
  Relocations v37/v38 have hashes `eccc5585...39ab` / `65710635...fd76`;
  their stable lineage hashes are `2c114075...e4ee` / `9a45c883...b6c1`.
  The first relocation attempt correctly rejected a missing seed boundary
  without partial mutation; copying the two hash-bound seeds made the retry
  pass.  Fresh stable-chain replays also passed: J326 checked 160 rounds,
  161 manifests, and 160 refinements, ending 1 VERIFIED / 1 UNKNOWN; its
  JSON/log hashes are `3950acde...400a` / `e6e4644c...f5da`.  J297 checked
  87 rounds, 88 manifests, and 174 refinements, ending 2 VERIFIED / 2 UNKNOWN;
  its hashes are `94cf2d68...19cd` / `fb7e9d2f...77c8`.  Three 3,600-second
  engines ran on the one J326 UNKNOWN and the two J297 UNKNOWNs.  The first
  launch stopped at argument validation because its scratch directories had
  not yet been created; those logs were retained, the directories were
  created, and all six wrappers were then verified running under new PIDs.
  All nine solver attempts ultimately returned UNKNOWN.  Result relocations
  v39/v40 have hashes `192fa3de...5e00` / `49177fb2...b321`.  Stable
  iGlucose/CaDiCaL/Kissat manifest hashes are `95272b5b...556d`,
  `65894fc2...6fbb`, `cc57ba98...cdb1` for J326 and
  `a4070bc4...214f`, `9e3b8f99...afec`, `504cfbf1...4ac5` for J297.  The
  three J326 audit logs each report 0 VERIFIED / 1 UNKNOWN with hash
  `3d9c660d...6508`; the J297 logs report 0 / 2 with hash
  `3f8f88e0...961b`.
  After the rescued-v6 parent audit finished, both live screened runners were
  resumed from states 522/418 and advanced at constant width through at least
  J326/J297 states 544/430.
- Paused both screened-runner parents again after observing that their two
  `screen-jobs=16` refiners can saturate all 64 logical CPUs and dilute the
  wall-time solver portfolios.  Exported J326 state 545 and J297 state 430;
  their ICNF/original-lineage hashes are `ebb59be3...a04e` /
  `ea2af0f2...6d84` and `a66d154e...6549` / `1d602960...f937`.
  Relocations v41/v42 have hashes `7068a6c2...73b8` /
  `20aaf6bb...05ac`, with stable lineage hashes `d5c1ac15...618b` /
  `049f5b15...57b8`.  Stable chain replays passed: J326 checked 183 rounds,
  184 manifests, and 183 refinements, ending 1 VERIFIED / 1 UNKNOWN, with
  JSON/log hashes `2b62b6ca...e909` / `0b964ed7...4db8`; J297 checked 99
  rounds, 100 manifests, and 198 refinements, ending 2 / 2, with hashes
  `d131a44d...cc06` / `b85b9044...2dce`.
- The first CPU-isolated 11-configuration, 7,200-second launch used the normal
  proof-first path.  Its 33 solver processes produced 5.4 GiB of incomplete
  raw DRAT in about four minutes, which would exhaust tmpfs before the time
  limit.  The 22 process groups were stopped and their explicitly named
  incomplete output/scratch directories removed; PID and runner logs were
  retained with `proof-first-aborted` suffixes.
- Added `--deferred-proof` to search with the proof stream discarded to
  `/dev/null`, then deterministically rerun only an UNSAT candidate to produce,
  compact, and check its proof.  Manifests bind the search log, both phase
  times, and rerun status; the independent auditor rejects inconsistent
  deferred metadata.  Real iGlucose/CaDiCaL/Kissat smoke manifests have hashes
  `8fd97583...07de`, `18cde709...3fee`, and `cf6e652d...9ded`; their identical
  audit-log hash is `0eee6d01...d90c` and all report 2 VERIFIED / 0 UNKNOWN.
  Regression tests also cover a search timeout and an UNSAT-candidate proof
  rerun timeout; both remain UNKNOWN without exposing an unchecked proof.
- Relaunched the same 11-configuration portfolio with deferred proof for
  7,200 seconds: iGlucose plus CaDiCaL and Kissat at seeds 0--4, totaling 11
  J326 and 22 J297 single-core processes under 22 wrappers.  After 15 seconds
  scratch use was only 88 MiB, consisting of materialized CNFs rather than raw
  proofs.  Both screened parents remain stopped while their completed child
  processes wait to be reaped.
- Added `analyze_screened_split_budget.py` to hash-check and replay completed
  two-solver screens at smaller time and variable budgets.  Replaying J326
  rounds 529--545 and J297 rounds 414--430 at 0.1 seconds over variables
  450--700 covers all 51 parent occurrences; every parent retains at least
  seven solver-agreed one-sided contradictions.  The analysis JSON/log hashes
  are `86ca692a...f4c` / `a0e53768...55c`.  This supports using a 0.1-second
  screen and the chain runner's 0.2-second proof quick pass in the next
  segment, but remains scheduling telemetry rather than an UNSAT certificate.
- Added `refine_queued_binary_cubes.py` so a chain can reuse several recorded
  candidate variables without repeating the full screen.  It selects only the
  first still-unassigned queued variable for each parent; the ordinary binary
  refinement audit and fresh per-child DRAT checks remain the source of proof
  validity, so stale or incorrect telemetry cannot silently close a branch.
- Replayed the exact state-545/state-430 screens over variables 450--700 and
  701--800.  They supplied 29+17 queued variables for J326 and at least 36+22
  per parent for J297; the two analysis JSON hashes are `28c4c7e4...3239` and
  `e6a8a307...f49a`.  All queued steps then produced fresh compact DRATs and
  kept the frontiers at one/two UNKNOWNs.  Stable segment replays reached J326
  states 574 and 591 with audit JSON/log hashes `0f3ef8b5...f6a2` /
  `4367814f...6aec` and `29550bc2...ba61` / `e3fc21b2...f950`; the relocation
  records are `077b8c3f...c1eb5` and `27d66247...55d1f`.  J297 reached states
  466 and 488 with audit hashes `7f7a2aad...0801` / `ae653aaf...bf29` and
  `042ba075...6d5` / `fd8993dd...2a5e`; its relocation records are
  `3e835d99...0c9c` and `29d5355d...3998`.
- Exported the deeper one/two-cube frontiers.  J326 ICNF/export-manifest hashes
  are `dd6e4889...b6e5` / `e5bf1be1...508f`; J297 hashes are
  `375ed2ee...5479` / `0195b6d0...f237`.  The state-545/state-430 deferred
  portfolio had run for about 34 minutes without a manifest or proof rerun;
  all 22 process groups were stopped after exact PID/PGID validation, and its
  PID/runner logs were retained with `superseded-r0591-r0488` suffixes.  The
  same 11 configurations were relaunched at states 591/488 as v3, again using
  22 wrappers and 33 solver processes with deferred proof generation.
- Replayed the original shallow screens at a one-second cutoff, recovering 22
  more queued J326 variables and at least 24 more variables per J297 parent.
  Fresh proof-mode runs and stable replays advanced J326 from state 591 to 613
  and J297 from 488 to 512, still at one/two UNKNOWN cubes.  The screen-analysis
  JSON/log hashes are `fd209349...d98` / `b44d837f...7a6`; the J326 audit
  JSON/log and relocation-v47 hashes are `b9268c7c...9e44` /
  `b0d70b5d...9853` / `70bc01b8...6a63`; the J297 values are
  `638c9e8a...c957` / `ca3929b1...8aa` / `ea72c39a...f40`.
- Fresh 0.3-second screens at states 613/512 found two common J326 candidates
  and 118/79 candidates for the two J297 parents; their combined analysis
  JSON/log hashes are `953b8c9c...f21d` / `4effa3d1...94ba`.  Checked queued
  segments then advanced J326 613--615 and J297 512--591.  The J326 audit
  JSON/log and relocation-v49 hashes are `e7b352b8...c559` /
  `0f9f8a7c...4ad` / `e2422c77...cbb`; the J297 values are
  `414605d3...3d1d` / `ad266ffb...adf2` / `a985e515...7b4`.
- A targeted state-591 J297 screen supplied one further common split.  Its
  checked segment reached state 592 with two VERIFIED / two UNKNOWN; the
  audit JSON/log and relocation-v51 hashes are `5f65a448...9c6e` /
  `1523ca6f...9e7f` / `c5f5812c...319b`.  A subsequent 0.3-second screen found
  no agreed candidate for the harder parent, and the J326 state-615 screen
  likewise found none.  This exhausts only those recorded cutoff/range
  probes, not the mathematical branches.
- The v3 and v4 deferred portfolios were superseded only after deeper checked
  frontiers became available; their PID/runner logs were retained with exact
  supersession suffixes.  The v5 portfolio is running at J326 state 615 and
  J297 state 591: 22 live wrappers drive 33 live single-core iGlucose,
  CaDiCaL, and Kissat searches.  The exported J326 ICNF/manifest hashes are
  `0e823802...c7e4` / `3dfa1bcf...c707`; J297 hashes are
  `2f7919ed...c7e4` / `dbe98b19...dd81`.  No search has yet emitted a result
  manifest or started the proof rerun.
- The independent four-segment/four-proof-job v7 complete-chain replay passed
  through J326 state 591 and J297 state 488.  Its bundle, audit JSON, and log
  hashes are `136074db...d1b`, `99e3b82c...1dda`, and
  `8766afca...9ee4`; the terminal counts are 1 VERIFIED / 1 UNKNOWN and
  2 / 2.  `all_cases_complete_unsat` remains false.
- After the failed-literal screens reached their local cutoff, ran one guarded
  CaDiCaL-lookahead growth round at nice 19.  J326 split on variable 5581 and
  grew 1--2 UNKNOWNs; its adopted-state and independent audit JSON/log hashes
  are `fa87326d...c128`, `99ddd8e6...4666`, and `b6d766dc...8db6`.
  J297 split on 7264 and -1140; one child produced a checked 0.118-second DRAT
  (`49bfd209...a54b`), leaving 1 VERIFIED / 3 UNKNOWN.  Its adopted state and
  audit JSON/log hashes are `13397257...cdad`, `b1328db4...4899`, and
  `13272947...e244`.  These are complete binary covers, not UNSAT claims.
- Generated final v9 chain/strengthened-parent candidates covering all queued
  and guarded-growth segments through J326 state 616 and J297 state 593.  The
  candidate hashes are `cb4047e1...b9da` / `ed0ad6ee...f9f0`.  The unused v8
  audit was stopped before producing a manifest, with its empty log/PID
  retained as superseded.
- CaDiCaL dynamic lookahead exposed useful auxiliary-variable splits beyond
  the screened primary range.  J326 advanced three constant-width rounds
  616--619 at 2 VERIFIED / 2 UNKNOWN, then adopted a guarded 2--3 growth at
  state 620.  The three-round audit JSON/log hashes are
  `42bcc27b...e89b` / `7115a317...c886`; the final-growth state and audit
  JSON/log hashes are `5f64f90d...7b84`, `8ec8b049...22c1`, and
  `75a7038d...91b4`.  J297 advanced two constant-width rounds from state 593,
  then adopted guarded 3--5 growth at 596.  Its final state and audit JSON/log
  hashes are `9cf105c3...fef0`, `323588f8...dc2e`, and
  `5551970e...15d2`.
- Stopped the six-minute v9 replay before it emitted a manifest and retained
  its empty log/PID as superseded.  Generated v10 chain/strengthened-parent
  candidates through J326/J297 states 620/596 with hashes
  `e866fb2d...17c9` / `cfda033c...8aff`; the v10 whole-chain replay is running
  at nice 10 and gates the parent replay on exact terminal counts 1 VERIFIED /
  3 UNKNOWN and 1 / 5.
- Exported the new 3/5-leaf frontiers.  J326 ICNF/frontier-manifest hashes are
  `0ab3839e...5574` / `4a244289...6304`; J297 hashes are
  `a88d7a86...de8e` / `4a86b965...d1ffb`.  Kept the original v5 portfolio
  running, added a nice-15 CaDiCaL seed-5 comparison on the earlier 2/3 leaves,
  and a nice-15 Kissat seed-5 comparison on these final 3/5 leaves.  All 13
  comparison solver children use deferred proof and have produced no result
  manifest or proof rerun yet.
- CaDiCaL seed 5 subsequently proved the middle cube of the earlier J297
  three-leaf frontier.  Search/proof rerun took 207.942/135.185 seconds and
  retained a 28,442,046-byte compact DRAT with hash `2cac4b77...fac2c`.
  The stable checkpoint and its independent replay-log hashes are
  `7e71ac80...3888` / `920c23df...ad5`; exact-family composition with the
  state-593 primary manifest produced 2 VERIFIED / 2 UNKNOWN.  The composed
  manifest/audit-log hashes are `48438b33...d6d` / `f57494e8...606`.
- The deferred-proof finalizer and both proof composers now preserve each
  completed row's search log.  The leaf auditor also accepts valid per-result
  deferred metadata in a mixed, top-level non-deferred composition while
  retaining the stricter global-deferred checks.  Regression tests cover all
  three paths; the full local and ARM suites passed 168/168 at that point.
- Kissat seed 5 also proved both descendants of that same J297 ancestor in the
  five-leaf dynamic frontier.  The first compact proof is 4,023,290 bytes with
  hash `a2df8c7c...a952`; the second is 33,892,870 bytes with hash
  `7be0d1c2...b8f5` after 170.466 seconds of search and 234.161 seconds of
  proof rerun.  Checkpoint v3 has manifest hash `e3e4ccb9...1c4f`, and its
  independent replay reports 2 VERIFIED / 3 UNKNOWN.  These are useful
  independent checks, but both descendants are already subsumed by the
  stronger ancestor proof.
- Restarted the guarded J297 chain from the audited ancestor composition.
  Two rounds retained 2 VERIFIED / 2 UNKNOWN through state 595; the next
  candidate grew to three UNKNOWNs and was rejected by the growth guard.  The
  state/final-manifest/halt hashes are `4df3cb42...8652`,
  `564e6003...54e3`, and `a8767ec3...ef4`; independent chain-audit JSON/log
  hashes are `872d5a74...7d58` / `92d307e0...dad2`.  Its exact two-leaf export
  has ICNF/manifest hashes `429c551b...f1b6` / `c8cbc477...6f89`, and a
  nice-15 Kissat seed-6 deferred run is live on those leaves.
- Generated v11 chain/strengthened-parent candidates with hashes
  `4dac9958...c1d` / `93ec6762...92e9`.  The whole-chain replay is running at
  nice 10 and gates the parent replay on terminal states J297 595 at 2/2 and
  J326 620 at 1/3.  The older v10 replay remains live so its already spent
  verification work is not discarded.
- The v10 replay later rejected the J297 v38 segment before emitting an audit
  manifest: all recorded hashes/counts matched, but the inner lineage used a
  repo-relative path while the bundle runner supplied the identical absolute
  path.  Its parent gate correctly refused to run.  Path comparisons in the
  chain auditor now accept only spellings that resolve to the same artifact;
  content hashes, counts, cube order, and proof replay remain strict.  Tests
  cover frontier, refinement, and adopted-growth records, bringing both full
  suites to 169/169.  A real absolute-path v38 replay passed with JSON/log
  hashes `3bf8be5f...d39a` / `98e0f8bb...ae72`.
- Because the first v11 process crossed this code update while running, it was
  retained only as a diagnostic and its parent watcher was superseded.  A
  fresh v11-normalized-v2 replay and exact-count-gated parent watcher now run
  entirely from commit `22e0f7f`.
- Continued both guarded growth branches at low priority.  J326 kept three
  UNKNOWNs for one round through state 621, then rejected a 3--5 candidate;
  its state/final-manifest/audit JSON/log hashes are `290b834d...fba`,
  `714fae25...e4a4`, `da8d2cdd...5c91`, and `91e2825b...d006`.
  J297's non-authoritative three-leaf branch stayed at width three for three
  rounds through state 599 before rejecting a 3--4 candidate.  Its matching
  hashes are `86a4977b...8621`, `42188f50...f596`,
  `b928e3cc...6f7c`, and `99b9bf98...2de1`; the two-leaf state 595 remains the
  authoritative J297 boundary.
- Exported the J326 state-621 and exploratory J297 state-599 frontiers.  Their
  ICNF/frontier-manifest hashes are `5b7e2021...b57a` /
  `3de094e2...af25` and `f540cd99...c2a6` / `dd3bfddb...3fe4`.
  Nice-15 Kissat seed-6 and iGlucose deferred portfolios now search those
  respective three-leaf families.
- Froze the formal candidate at v12 rather than completing an immediately
  stale v11 replay.  Both v11 audits were stopped before a manifest and their
  logs/PIDs retained with `superseded-by-v12-before-manifest`.  The v12
  chain/strengthened-parent hashes are `99e18614...edb5` /
  `c8b7a07d...2f1e`; one clean whole-chain replay now gates its parent on exact
  terminal rounds 595/621, VERIFIED counts 2/3, and UNKNOWN counts 2/3.
- One further exploratory step immediately grew J326 5--6 and J297 4--5, so
  neither wider branch was continued.  Added low-priority CaDiCaL seed-7
  searches on the frozen J297 two-leaf and J326 three-leaf frontiers, filling
  the remaining logical CPUs without displacing higher-priority replay work.
- The clean v12 whole-chain replay passed after about 54 minutes.  Its audit
  JSON/log hashes are `42b677ef...e0a` / `0500d381...320`; it records 25 J297
  and 28 J326 segments, exact terminal rounds 595/621, VERIFIED 2/3, UNKNOWN
  2/3, and `all_cases_complete_unsat=false`.  Only after this exact gate
  passed did the watcher start the strengthened-parent audit, which is now
  independently replaying the chain and backbone connections.
- The original v5 7200-second portfolio finished all 22 runs / 33 solver
  attempts without an UNSAT result or deferred proof rerun.  J326 state 615
  therefore remains 0 VERIFIED / 1 UNKNOWN across 11 configurations, and J297
  state 591 remains 0 / 2.  Copied all terminal manifests from tmpfs into the
  persistent v33/v34 `runs/` directories, composed each identical-cube
  11-source portfolio, and independently audited all 22 raw manifests plus
  both compositions.  The J326/J297 composed-manifest hashes are
  `162bc246...503` / `0b4923d2...707`; their audit-log hashes are
  `3d9c660d...508` / `3f8f88e0...961`.  Recorded attempt time totals are
  80,721.433701 and 161,442.883602 solver-seconds respectively.  This is
  negative scheduling evidence, not an UNSAT certificate.
- The state-620 J326 Kissat seed-5 comparison closed two of three UNKNOWN
  parents.  The 174,534,017 / 176,834,884-byte compact DRAT hashes are
  `18d4ca99...742` / `6277c86f...cbd`; the persistent checkpoint manifest and
  independent audit-log hashes are `9abf9211...22e` / `ef6e9aab...5ad`.
  Exact-family composition with the prior state-620 manifest has hash
  `e71c5ea1...692` and leaves 3 VERIFIED / 1 UNKNOWN.
- Continued that rescued J326 boundary for one guarded round.  CaDiCaL split
  the sole remaining parent on variable 6498 and closed one child with a
  79,371-byte DRAT (`cbafb9b8...fd9`), leaving 1 VERIFIED / 1 UNKNOWN at state
  621.  State/final-manifest hashes are `4b3d488a...ca3` /
  `7d657b87...c67`; independent chain-audit JSON/log hashes are
  `2e761824...5b6b` / `d99f98e7...0ac8`.  This reduces the certified J326
  frontier from three UNKNOWNs to one without claiming J326 UNSAT.
- The older variable-9448 split and new variable-6498 split expose distinct
  UNKNOWN descendants (`2ff9dd8e...` and `8f640a56...`).  Kept the existing
  searches and launched low-priority iGlucose/Kissat/CaDiCaL portfolios on
  both single leaves.  Their old-leaf ICNF/selection hashes are
  `30757674...264` / `c1f3c743...c52`; new-leaf ICNF/frontier hashes are
  `d97568c4...234` / `e4553bcc...488`.
- Generated v13 chain/strengthened-parent candidates with hashes
  `01996f77...2ad7` / `28845912...5f9e`.  Its clean whole-chain replay is now
  running and should end at J297 two UNKNOWN / J326 one UNKNOWN; v12 remains
  the latest accepted full-chain audit until this finishes.
- The completed J297 state-593 CaDiCaL seed-5 run retained the already known
  middle-parent proof and timed out the other two leaves.  Its final manifest
  hash is `85604980...25f9`, and independent replay again passed 1 VERIFIED /
  2 UNKNOWN.  This is retained telemetry and does not improve the state-595
  two-leaf authoritative boundary.
- The v12 strengthened-parent audit completed after independently checking
  both full chains, 193 J297 and 178 J326 false-polarity backbone proofs, and
  both parent-plus-15-backbone bindings.  Audit JSON/log hashes are
  `ddc04bfe...9336` / `a2ce2744...db52`.  The terminal counts remain J297
  2 VERIFIED / 2 UNKNOWN and J326 3 / 3, so both `parent_unsat` flags and
  `all_parents_unsat` are false as required for an honest partial result.
- The v13 whole-chain audit passed its exact gate.  Bundle/audit/log hashes
  are `01996f77...2ad7` / `ae4e7cb2...a74` / `8299797e...18d`; its 25/28
  segment replays end at J297 state 595 with 2 VERIFIED / 2 UNKNOWN and J326
  state 621 with 1 / 1.  The gated v13 strengthened-parent replay is now
  running at nice 10; v12 remains the latest parent-audited candidate.
- A J326 state-616 CaDiCaL seed-5 comparison retained a 227,111,943-byte DRAT
  (`8d29f3af...190`) and passed independent replay at 1 VERIFIED / 1 UNKNOWN.
  Final-manifest/audit-log hashes are `b7632ad2...318` / `ea389d56...45a9`.
  It closes the sibling ancestor branch, not the current state-621 UNKNOWN,
  so the authoritative J326 width remains one.
- Kissat seed 5 closed one more child of the alternative J297 state-596
  family with a 256,128,846-byte DRAT (`2aa0eaa9...10`).  Its final manifest
  and independent audit-log hashes are `9bfa3563...655` / `78cce30e...fb9`.
  Exact-family composition has hash `a80e3eee...582` and leaves two UNKNOWN.
- Continued that composed J297 family for three checked constant-width rounds
  through state 599, then rejected a 2--4 growth.  State/final/halt hashes are
  `a3a06907...cfb` / `b130bfac...c24` / `c5dcf2de...394`; the independent
  four-manifest/six-refinement chain audit passed with JSON/log hashes
  `518a5f5d...bd9` / `e9d2bb8b...233`.  It is a deeper two-leaf alternative,
  not a width reduction.  Its export hashes are `112c1dff...629e` /
  `444783cd...a3e`, and three low-priority solver families search both leaves.
- Prepared v14 chain/parent candidates using the audited J297 state-599 route
  and rescued J326 state-621 route.  Their hashes are `d292cc97...94d` /
  `29611d8b...20e`.  Replay is deferred until the running v13 parent audit
  releases proof-checking capacity; no v14 audit result is claimed yet.

No parent-1 UNSAT, fixed-pair UNSAT, order-45 UNSAT, or `R(5,5) <= 45`
theorem is claimed.
