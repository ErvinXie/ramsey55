# 2026-08-14

## 同步检查点

- 暂停新增计算，准备首次公开仓库同步。
- ARM builder 上的固定 H#35/A#0 搜索稳定状态为 round 185：累计闭合
  1,690,883 个计算分支，frontier 256，主变量分布
  `{0:144, 4:12, 13:68, 18:32}`，状态 SHA-256 为
  `b50748229e415eec7055caf4a4946101df887cb315cd5c042da80fb077fbd66a`。
- round 186 的第四列计算已经完整落盘：13,888 行连续，13,847 UNSAT、
  41 UNKNOWN、0 SAT；结果尚未接纳，因此 round 185 仍是可恢复检查点。
- 下一步先接纳 round 186 的 434 个完整 Cartesian 组，并将 41 个 UNKNOWN
  展开到第五列；之后处理 survivor=32 的最后 18 个第三层父节点。
- 固定 pair 闭合后，优先做 proof-producing DRAT/LRAT 小规模回放和独立
  检查，再决定全部 62,382 个 H128/A85 配对的批量调度方案。

## 转向上界 45

- 目标改为证明 \(R(5,5)\le45\)。利用 \(R(4,5)=25\)、握手奇偶、补图和
  重标号，把任意假想 45 点反例完整归约到固定星度数 20、22 两个 SAT
  分支；这里不再需要度数 21 分支。
- 在 ARM 节点生成两个精确 DIMACS：各 990 变量、2,443,562 子句、约
  107 MiB。独立 verifier 逐子句重构并核对通过；哈希已写入
  `data/order45-benchmark-manifest.json`。
- CaDiCaL/Kissat 对两个原始 CNF 的 60 秒探测均超时，没有 SAT 解或 UNSAT
  结论。Kissat 分别推进约 40 万和 20 万 conflicts，说明基准可正常加载，
  但直接 CDCL 不是短路径。
- 将三点 excess 恒等式专门化到 45 点，形式化算术常数为
  `[452,443,440,443,452]`。注意 excess 的非正见证仍可能是奇度，因此
  局部目录分析必须覆盖 20+24、21+23、22+22 三类，而不是只看 SAT 的
  20/22 规范分支。
- 对 McKay `r45extreme` 目录做精确边层审计：三类分别只覆盖 28 个可行
  边数对中的 18 个、36 个中的 8 个、45 个有序对中的 4 个；已覆盖层在
  gluing 前仍有 316,734,625、3,481,603,081、967,769,881 个原始目录图对。
- 将 ambient=45 的精确有理 local LP 跑到全部 6 点诱导类型，度数 20--24
  的贡献区间都宽幅跨过零。旧恒等式加边数范围和至多六点局部分布不足以
  分离，需要新的重叠约束、整邻域优化或新计数恒等式。
- ARM 节点有 64 逻辑 CPU、244 GiB RAM、约 194 GiB 空闲磁盘，足够继续
  理论过滤器和证书原型；当前首要瓶颈是结构压缩，而不是增加同类机器。
- 新增规范化模块、CNF 生成/独立核验、目录覆盖审计、Lean 算术层和测试。
  在 ARM 节点通过其现有 Mihomo 代理取得 Lean 4.31 ARM64 官方包；文件大小
  556,761,824 字节、SHA-256
  `b1bf1d3c586b76cf4a86212a595d8b9edd99f438a41cce85d5780fa9347c811b`
  与 GitHub 官方 release API 一致。`lake build` 随后完成 74/74 个目标，新增
  order-45 算术定理已通过内核检查。
- 用户提供的 `xwy-proxy.yaml` 已以 0600 权限部署到 ARM 的
  `/root/.config/mihomo/xwy-proxy.yaml` 并通过 Mihomo 语法检查。远端已有可用
  的 `/etc/mihomo` 实例，故没有覆盖或重启现有服务；所有后续计算仍在 ARM。

## 全顶点度数窗口强化

- 原始 order-45 CNF 只固定一个星，没有显式编码其他 44 个顶点的已知度数
  范围。新增双向 at-least sequential counter，把每个度数限制到 20--24。
  编码在 1--4 个输入上穷尽全部主变量与辅助变量赋值，所有范围均符合语义。
- 两个强化 CNF 各有 36,190 变量、2,581,414 子句。独立 counter 实现逐条
  重构通过；通用 DIMACS verifier 另行确认原始 2,443,562 个子句是精确
  前缀，恰好增加 35,200 个变量和 137,852 个子句。
- 强化分支 SHA-256 分别为
  `e3e0ec995135e1aa3f36bc256b8c72c78fd3357a31372ccd848a19aaba345174`
  与 `c643341fcc364b766724df5ee2a6d7d0db5109bb853bfdfaf787ff8c51aca211`。
- Kissat 60 秒仍未闭合。d20/d22 分别得到 302,578/138,862 conflicts、
  1,225,210/1,159,811 decisions 和 64,654,468/56,548,646 propagations。
  相比原始公式传播量显著上升，但不同编码间的冲突数不能当作接近 UNSAT
  的证据。

## 固定星交叉行对称破缺

- 固定 apex 后仍可任意置换其邻域，因此可选择邻域到非邻域的交叉行按
  Boolean lex 非降排列。只排序这一侧；同时独立排序另一侧列一般不保持行序。
- 新增双向 prefix-equality lex 编码，并穷尽检查至宽度 4 的全部主/辅助
  赋值。独立 verifier 对两个完整公式逐条重构通过。
- d20/d22 公式分别为 36,627/36,631 变量和 2,584,036/2,584,060 子句，
  SHA-256 为
  `656b31f45a887c255f8a4ce181cb08e1b89675484ee833b58c03db69bf0558f3`
  与 `f3e834bab124dc70761e3c33b4461e5d89862cb68b797bfc8e939ec80d22989f`。
- 60 秒 Kissat 仍 UNKNOWN；d20/d22 的 decisions 为 995,991/1,129,574，
  propagations 为 60,859,890/59,205,375。对称约束已被使用，但没有证明性
  结论。

## 非正 excess 见证完整覆盖

- 把全局三点恒等式的非正见证经补图规范化为 d20/d21/d22 三支，并分别
  编码局部阈值 226/222/220。这里三支合起来是完整覆盖。
- 新增支持负文字的双向基数 counter；语义测试覆盖至 4 输入，excess 恒等式
  穷尽检查所有至 5 点图。独立 verifier 逐条重构三个完整 CNF。
- 三支变量数为 116,518/114,885/114,181，子句数为
  2,902,909/2,896,390/2,893,579；60 秒 Kissat 均 UNKNOWN。

## 精确局部边数 strata

- 用已知 \(R(4,5,n)\) 精确边范围为 H/J 建立两个可观察双向 counters，把
  excess 覆盖拆为 d20/d21/d22 的 28/36/45 个互斥 edge-pair cubes。
- 独立 verifier 重构三个母公式、counter 输出和全部 cube literals，并确认
  cube 数及覆盖边数对完全一致。母公式约 76k--79k 变量、2.74M--2.75M 子句。
- 新增 CaDiCaL assumptions 扫描器，一个母公式只加载一次。10,000
  conflicts/cube 下 109 个 cube 全部 UNKNOWN；复用扫描只作难度排序，最终
  UNSAT 必须单 cube 独立输出并检查 proof。

## 固定 H100/J132 顶层

- 把 `r45extreme` 中唯一的 20 点 100 边图复制为稳定参考文件
  `data/reference/r4520.100.g6`，并重新生成/独立验证固定 H、J 至少 126 边
  的 d20 母公式：63,091 变量、2,688,807 子句，SHA-256
  `6496cd1f444ea7da882d57717f56740114ba382d47cd915ff38df8121aaa3734`。
- 完整 24 点目录中 132 边层恰有两个代表，索引 297775、326185。固定 H/J
  后只剩 480 个交叉主变量；无对称公式分别为 9,746 变量、114,968/115,088
  子句，SHA-256 为 `61a21a...a4625`、`e783d6...78d6`。独立 Python 实现
  逐条重构 Ramsey 子句、度数 counter 和全部 DIMACS，通过精确比对。
- Kissat、CaDiCaL、Minisat 的多个 600 秒单体运行均 UNKNOWN。CaDiCaL
  原生深度 14 cuber 各生成完整 16,384 cubes；0.1 秒并行扫描只剩 23/20
  UNKNOWN，10 秒层剩 2/3，120 秒层再各剩 1。没有任何 SAT。
- March 深度 10/12/14 与手工 adaptive split 的关闭率明显较差；低预算
  反复 split 会让难 frontier 小幅增长。CaDiCaL 自身 cubes 是当前首选。
- 新增 `prove_cadical_cubes.cpp`：同一 solver 中对 assumptions 求 UNSAT，
  调用 `conclude()` 把失败核心的否定子句写入同一 binary DRAT；到冲突预算
  时用 lookahead 递归二分，最后必须无 assumptions 导出空子句。小型 smoke
  公式已经 `drat-trim` 验证。大型两图对证书尚在生成，不能据此宣称该层
  已证明 UNSAT，更不能宣称 (R(5,5)\le45)。
- CaDiCaL 源码确认 probing lookahead 没有传播上限；证明器现可通过官方
  `Terminator` 回调限制单次 lookahead 墙钟时间，并在超时后继续使用当前
  occurrence-ranked 分支变量。小型 binary DRAT 仍由 `drat-trim` 接受。
  另加可选主变量上限：若 lookahead 选择 counter auxiliary，则改用出现次数
  最高且尚未假设的 1--480 交叉边；5 秒真实 CNF 诊断的 49 次动态分裂全部
  落在该范围。
  同一 terminator 也可选择限制单次 `solve()` 墙钟时间。真实诊断发现并修复
  了扩展命令行后 `argc == 8/9` 使较长参数表关闭旧选项的问题；改为
  `argc >= 8/9` 后，20 秒诊断的 10 次动态分裂全部在 1--480。
- 新增可并行的单 cube 叶证书路径。原型在 SAT 母公式、UNSAT cube 上由
  `conclude()` 导出 blocking clause，并对增强 CNF 追加空步；`drat-trim`
  接受母公式加 cube 单位子句、拒绝未增强的母公式。动态 root-index 模式
  也通过 1 次 split/3 次 solve 的同类烟测。三个实际 2.74M--2.75M 子句
  strata cube 的 120 秒无界探针均未完成，下一步改用有界动态分裂叶证明。
- 新增 leaf collection auditor：绑定 edge-strata manifest/CNF/cube 哈希，
  检查单根结果树与运行参数，临时重建 cube-augmented CNF 并逐份 replay；
  默认缺一份即失败，`--allow-partial` 只输出显式不完整清单。
  新增证明审计器，检查公式/cube 哈希、完整二叉结果森林、运行参数、证明
  哈希与逐式 replay；大型 proof portfolio 仍在 ARM 上运行。
- ARM 回归为 74/74 Python 测试、Lean 74/74；远端代理继续由 systemd 上的
  Mihomo 直接提供，不经过本机 SSH 转发。
