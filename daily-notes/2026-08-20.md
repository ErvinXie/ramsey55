# 2026-08-20

## Order-45 parent-1 strengthened lookahead

- Recovered the completed August 16 single-root runs from ARM tmpfs. Their
  strengthened parent-1 frontiers were far narrower than the distributed
  17-leaf continuation: J297775 reached UNKNOWN widths 1/2/3/4/5 at state
  rounds 0/2/9/28/74, while J326185 reached 1/2/3/4 at rounds 0/5/37/128.
- Added a guarded-growth adoption tool. It rejects SAT or inconsistent
  counts, verifies both materialized proof schemas, and binds the halt plus
  parent/candidate manifests by SHA-256. Extended the independent chain
  auditor to verify this metadata against the reconstructed last transition.
- Independently replayed every recovered normal segment and every growth
  transition. The first stable checkpoint ends at state round 132 with six
  J297775 UNKNOWN cubes and five J326185 UNKNOWN cubes. Across six and five
  segments, respectively, the stable top-level replay accepted 138/137 proof
  manifests and reconstructed 547/358 parent refinements. It deliberately
  reports `all_cases_complete_unsat=false`.
- Added a chain-bundle auditor that checks cross-segment adjacency and proves
  that each one-row root is exactly the selected strengthened ICNF row. It
  also checks the upstream lineage maps row zero to fixed-pair parent 1 and
  carries 15 certified backbone literals. The root hashes are
  `b8a29a95...e912` and `715b4a45...53b9`.
- Added a deterministic proof-tree relocator. The first stable migration from
  `/dev/shm` to
  `build/order45-fixed-pairs/final-parent1-strengthened-single-root-lookahead-v1`
  rewrote 2,073 paths and propagated 353 dependent hashes across 878 JSON
  files in two convergence passes. A fresh replay from only the stable paths
  passed.
- At the next J326185 growth, 600-second CaDiCaL `--unsat` and Kissat
  `--unsat` independently closed the same hard child. Their compact checked
  proofs are 90,468,992 and 93,690,807 bytes. The Kissat proof was composed
  into the round-131 candidate, reducing its UNKNOWN count from five to four,
  and a new exact-cube retry chain is running from state round 132.
- The corresponding 600-second J297775 round-131 rescue left both children
  UNKNOWN under both solvers. Its guarded growth to width seven at round 134
  was independently replayed; that width then held through round 164 before
  an audited growth to eight. CaDiCaL, Kissat, and iGlucose all left the new
  growth pair 2/2 UNKNOWN at 600 seconds. The width-eight continuation uses a
  600-second Kissat fallback only on double-UNKNOWN sibling pairs.
- Added and ran a higher-level parent-reduction auditor. It replayed all 193
  J297775 and 178 J326185 full-screen backbone proofs, selected the 15 facts
  for parent 1 in each case, checked that the strengthened row is exactly the
  original parent plus those literals, and then replayed the chain bundle.
  It certifies reductions of the original parents to six/five UNKNOWN cubes
  while explicitly returning `all_parents_unsat=false`. The final audit
  manifest SHA-256 is
  `a8be4397e902f50c261a8d2a9fab4199535cfc1786b509a6caa720c125d41d29`.
- Added or extended unit coverage for growth adoption, relocation hash
  propagation, selected-root lineage, and explicit partial-claim semantics.
  The complete local suite initially passed 141 tests.
- Stabilized a separately named rescued checkpoint without changing the
  published baseline bundle. J297775 now replays eight segments, 173 proof
  manifests, and 776 refinements through state round 165, leaving eight
  UNKNOWN cubes. J326185 replays six segments, 169 manifests, and 482
  refinements through state round 163, leaving five UNKNOWN cubes after an
  independently certified exact-cube retry temporarily reduced the frontier
  to four.
- Replayed the rescued checkpoint from stable storage and then composed it
  from the original fixed-pair parent-1 cubes through all 371 backbone proofs.
  The chain audit hashes are `a640c4dd...36fa8` / `ca9325c3...5dda`; the
  parent-composition audit hashes are `ed9e04eb...35d63` /
  `24da5f48...5907`. Both explicitly report false completion claims.
- Stabilized rescued-v2 at the next two audited growth boundaries. J297775
  reaches state round 191 with nine segments, 200 manifests, 984 refinements,
  and nine UNKNOWN cubes. J326185 reaches state round 240 with seven segments,
  247 manifests, 867 refinements, and six UNKNOWN cubes. The stable chain
  audit hashes are `801348f8...156b` / `ebe86aae...aea6`; the original-parent
  composition audit hashes are `51f6d5ba...fbd8` / `38f75440...0b29`. Both
  completion flags remain false.
- Cross-validated two live fallback closures. At J297775 round 173, Kissat
  and CaDiCaL produced independent checked compact DRATs of 41,294,002 and
  62,412,884 bytes for the same child. At J326185 round 169, Kissat, CaDiCaL,
  and iGlucose produced independent checked proofs of 66,611,040, 54,089,886,
  and 104,704,389 bytes. These prevented frontier growth at those rounds.
- A guarded relocation refusal exposed partial writes before missing-file
  validation. Reworked the relocator to compute path replacements and
  dependent JSON hashes in memory, validate the complete virtual tree, and
  only then write changed files. Added a regression test proving that a
  missing hash-bound target leaves the input byte-for-byte unchanged. The
  complete local suite now passes 142 tests.
- Added an opt-in exact unordered UNKNOWN-subset composition mode. It rejects
  duplicate, SAT, or non-UNKNOWN replacements and records the secondary to
  primary row map. This was needed because the seven J297775 round-190 rescue
  cubes were sorted in reverse primary order. The complete local suite now
  passes 143 tests.
- Kissat closed all seven J297775 round-190 UNKNOWN cubes outside the growth
  pair with 1,178,631,163 bytes of checked compact DRAT. Independent replay of
  the composed 16-row manifest accepted 14 proofs and left two UNKNOWN. For
  J326185, Kissat closed the other four round-239 cubes, then CaDiCaL and
  Kissat independently closed the same member of the remaining pair with
  195,953,015- and 270,149,972-byte proofs. The smaller proof produced a
  ten-row manifest with nine verified and one UNKNOWN; independent replay
  passed.
- Stabilized rescued-v3 at state rounds 201/241. J297775 now replays ten
  segments, 211 proof manifests, and 1,004 refinements to two UNKNOWN cubes;
  J326185 replays eight segments, 249 manifests, and 868 refinements to one.
  The chain audit JSON/log hashes are `999e416e...3342f` /
  `4c18a322...9c7a9`. The original-parent composition replayed all 371
  backbone proofs and has audit/log hashes `cb1b3911...f8dc` /
  `2d157f5d...ba23`. Both explicitly return false completion claims.
- The v9 transactional relocation scanned 1,587 JSON documents, rewrote 99
  paths, propagated 20 hashes in three passes, and has record hash
  `5fa5abd2...37001`. The stable proof tree is 5.5 GB; the ARM root disk has
  25 GB free and tmpfs has 111 GB free.
- Rescued the J297775 growth boundaries at rounds 207, 218, and 238. The
  round-238 child was independently proved by CaDiCaL and Kissat with compact
  DRATs of 100,709,025 and 86,486,471 bytes. The stable width-two chain now
  reaches state round 242. Rescued J326185 at rounds 257 and 277, reducing both
  growth pairs back to one UNKNOWN; the latter closure has independent
  80,547,438-byte Kissat and 70,463,276-byte CaDiCaL proofs.
- Stabilized rescued-v4 with 13/10 J297775/J326185 segments through state
  rounds 242/278. Fresh replay audits 255/288 proof manifests and 1,086/905
  parent refinements, leaving two/one UNKNOWN. The chain audit JSON/log hashes
  are `24a0698...5542c` / `2e7bbe57...f9eb`; the original-parent audit again
  checks all 371 backbone proofs and has JSON/log hashes
  `968bbbe6...3c34` / `a6f03058...2536`. Both completion flags are false.
- Fixed the proof producer to atomically flush collected results when a worker
  fails or is interrupted. Fixed relocation to propagate hashes for
  repository-relative paths without rewriting older relocation records, and
  made chain-state path comparison independent of absolute versus
  repository-relative spelling while retaining hash and content checks. The
  local and ARM suites both pass 147 tests.
- Transactional relocation v10 remains unchanged at hash `bab0d6c0...ca5`.
  Corrective v11 changed six paths/five hash bindings and has hash
  `bd1edf2f...0d6c`; post-freeze v12 changed 38 paths/eight bindings across
  2,007 JSON documents and has hash `9225ceb3...ca5`. The stable tree is 8.2
  GB and the ARM root disk retains about 22 GB free.
- After the v4 freeze, CaDiCaL, Kissat, and iGlucose independently closed the
  same J297775 round-242 child. The 112,210,091-byte Kissat proof was composed
  and replayed; width two holds through round 243 and the guarded round-244
  candidate grows to three. At J326185 round 278, CaDiCaL and Kissat produced
  independent 94,139,627- and 101,863,998-byte proofs. The CaDiCaL composition
  replayed successfully. A stable snapshot through state round 351 replays 73
  manifests and 72 refinements while leaving one UNKNOWN; its audit JSON/log
  hashes are `d2a08c1e...58dbb` / `61490d91...23b1e`. Relocation v13 has hash
  `a47f0cac...d367e`, and the live width-one chain continues beyond the
  snapshot.

No parent-1 UNSAT, fixed-pair UNSAT, order-45 UNSAT, or `R(5,5) <= 45` theorem
is claimed. The next certificate step is to rescue the J297775 round-244
growth candidate, preserve and continue the J326185 single-cube chain, and
extend the same independently replayable coverage beyond these two
strengthened parent-1 cases.
