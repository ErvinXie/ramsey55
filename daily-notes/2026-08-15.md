# 2026-08-15

## Order-45 certificate continuation

- Extended the hash-bound leaf pilot to a fourth 120-second configuration.
  The 30,000-conflict auxiliary run left one open leaf for each representative
  d20/d21/d22 cube, at maximum depths 4/5/5. It is UNKNOWN telemetry, not an
  UNSAT result.
- Paused the inferior 10,000-conflict auxiliary production processes
  recoverably and launched no-timeout 30,000-conflict replacements for
  d20/c25, d21/c26, and d22/c15. Each wrapper will materialize the
  cube-augmented CNF and run `drat-trim` only after runner status 20.
- The fixed H100/J132 unified proofs and their automatic checker watchers
  remain active. Neither the fixed layer nor the full order-45 theorem is yet
  claimed proved.
- Audited the root-only runner argument path and found that its 11-argument
  form ignored the requested per-solve deadline. The incomplete production
  artifacts remain valid telemetry but are not certificates. Corrected the
  parser, made the runner log its effective parameters, and made the leaf
  auditor reject missing or mismatched parameter records.
- Paused the six affected production processes and launched corrected
  replacements for d20/c25, d21/c26, d22/c15 and the extreme d20/c27,
  d21/c35, d22/c44 cases. The ARM binary SHA-256 is
  `e84cec00f80594babb89f24079624c930e80121ce637f0f4a78db296d54336a4`;
  all six logs independently record an effective 10-second solve cap.
- The corrected remote smoke test passed, including independent DRAT replay
  and rejection against the base-only formula, and the Python suite passed
  77/77 tests on the ARM builder.
- Launched one corrected unified proof stream for each d20/d21/d22 mother CNF.
  This route reuses learned clauses across all 28/36/45 roots and, if it
  closes, requires three base-formula DRAT replays instead of 109 separate
  cube-augmented replays. Added a dedicated strict auditor for this route.
- Generated and independently reconstructed the optional symmetry-reduced
  H100/J132 formulas. Their H/J automorphism counts are 4x24 and 4x48. Started
  bounded CaDiCaL/Kissat telemetry and added a Lean theorem showing that every
  nonempty finite transformation-closed orbit has a least representative
  satisfying all listed lex-leader inequalities. The concrete CNF-invariance
  bridge remains pending, so no labelled UNSAT claim uses symmetry yet.
- Remote `lake build` succeeds with 76 jobs. The new orbit theorem reports
  only Lean's standard `propext`, `Classical.choice`, and `Quot.sound` axioms;
  it uses neither `sorry` nor `native_decide`.
- Extended the fixed-pair family generator and independent manifest verifier
  to support an explicit `--symmetry` mode. The committed manifest binds both
  lex CNFs, their 4x24/4x48 automorphism counts, exact dimensions, and hashes;
  the proof auditor marks every symmetry result as requiring the orbit bridge.
  The full ARM Python suite passes 78/78 tests.
- The symmetry search comparison was negative. All four 600-second
  CaDiCaL/Kissat monolithic runs timed out. J297775 depth-10 cubing left
  19/10/3 UNKNOWN cases at 0.1/10/120 seconds, while J326185 cubing itself
  timed out at 180 seconds. Stopped two depth-14 cubers after more than 650
  seconds without output and committed the hash-bound UNKNOWN telemetry; the
  no-symmetry route remains production. After adding the manifest/telemetry
  boundary tests, the current full ARM Python suite passes 80/80.
- Replaced unconditional freezing of every proof-runner variable with a
  correctness-preserving selective policy: freeze all input-cube variables,
  the requested primary range, and each dynamic split immediately before it
  becomes an assumption. The DRAT smoke test still passes and base-only replay
  still fails as required. A hash-bound 120-second pilot showed no useful
  edge-stratum speedup, but fixed-pair runs froze only 494 of 9,746 variables
  and achieved 84/86 tail splits after scanning all 16,384 roots.
- Launched the selective-freeze fixed-pair pair as an additional no-timeout
  production route with automatic `drat-trim` and strict final audit. The
  auditor now recomputes the initial frozen-variable union from the cube file
  and rejects a mismatched policy/configuration log. Current ARM Python suite:
  82/82; Lean build: 76/76 jobs.
- A second selective-freeze fixed-pair pilot left the primary bound at zero,
  freezing only the 14 variables appearing in the depth-14 cube family. In
  120 seconds it reached 93/105 splits with only four open branches for each
  formula, versus 84/86 splits and 20/18 open branches when freezing all 480
  cross edges. Launched this primary-0 configuration as the preferred
  no-timeout route with its own replay and strict-audit watchers.
- Compared primary-0 budgets at 30,000 and 100,000 conflicts. Their 120-second
  frontiers were 4/4 and 3/4, while the 10,000 route retained the highest
  split throughput. Kept 10,000 as the main route and launched only the
  100,000/20-second pair as a small-frontier hedge; every completion path has
  independent replay and a selective-policy audit watcher.
- Paused eight fixed-pair solver processes dominated by the selective
  primary-0 routes. Their incomplete DRAT/TSV files remain intact and the
  processes are resumable. The active portfolio is now nine edge-stratum
  solvers plus four fixed-pair solvers, reducing ongoing proof I/O.
- Extended the Lean symmetry bridge with a direct UNSAT transfer theorem:
  if every base solution has a finite transformation-closed satisfying orbit
  and the lex leaders are impossible, then the unsymmetrized predicate is
  impossible. Concrete fixed-pair invariance and DIMACS correspondence remain
  explicit hypotheses rather than hidden trust.
- Added `tools/export_proof_frontier.py`, which reconstructs the exact open
  preorder-DFS frontier from a live flushed proof-runner TSV. It validates the
  global attempt sequence, monotone multi-root order, signed split literals,
  depths, statuses, repeated variables, and the binary-tree balance identity.
  An explicit stack avoids the runner's possible depth-1024 recursion limit;
  five focused tests cover root-only and multi-root logs plus live partial rows.
- Used immutable snapshots rather than disturbing the 13 active proof
  processes. For edge strata the reconstructed frontiers were d20 unified 2,
  d20/c27 22, and d22/c15 6. Independent 120-second scans closed 0, 20, and 4
  respectively, leaving 2/2/2 genuinely hard cubes and finding no SAT model.
  The d20 unified cubes had reached 512 literals, exposing the legacy
  freeze-all runner's long auxiliary-variable chain.
- Compared the two d20 hard cubes under the selective runner. Free auxiliary
  splitting left 1/2 branches after 120 seconds, whereas forcing variables
  1--990 to graph edges widened the frontier to 10/10. The structural bound is
  therefore not promoted to production. The current legacy proof streams stay
  active, while a later production hedge should prefer selective free splits.
- Reconstructed the fixed-pair multi-root tails after all first 16,383 roots
  had closed. The four snapshots contained 7/4/4/1 open cubes; independent
  120-second scans closed 4/2/2/0 and left 3/2/2/1 UNKNOWN. The final single
  J326185 cube is therefore a real hard core, not a stale pending sibling.
  All seven scans and their live snapshots are hash-bound in
  `data/order45-proof-frontier-pilot.json` and explicitly make no UNSAT claim.
- ARM regression with the correct `PYTHONPATH=src` invocation passes 87/87
  Python tests. An earlier discovery invocation without that path produced
  import errors only; it is not counted as a project test result.
- Extended both edge-stratum proof auditors with a declared
  `legacy-all|selective` freeze policy. In selective mode they independently
  recompute the union of every input-cube variable and the requested primary
  range, then require the runner's logged `initial_frozen_variables` to match.
  Two mismatch/acceptance tests raise the ARM regression total to 90/90.
- Started three additional selective unified proof streams in
  `build/order45-strata/unified-selective-b30000-c128000-la1-primary0-solve10`.
  Their logged initial freeze counts are 16/18/20 for d20/d21/d22, using the
  same 30,000--128,000 conflict bounds, 1-second lookahead cap, and 10-second
  solve cap as the legacy streams. The old nine edge processes remain active;
  this is a three-core hedge on the 64-core/244-GiB ARM builder, not a restart.
- Added `scripts/run_order45_strata_unified_selective_proof.sh` so the exact
  production sequence is reproducible. It records the runner exit, refuses to
  proceed unless it is 20, then replays binary DRAT against the mother CNF and
  writes the strict hash-bound audit manifest. No selective stream has yet
  returned 20, so this launch does not change the theorem status.
- Corrected the formal cube-composition interface for structural strata.
  `CnfCubeFamilyCoversFormula` now requires coverage only for assignments that
  satisfy the mother CNF; `cnfFormulaIsUnsat_of_relativeCubeCover` turns that
  cover plus UNSAT leaves into mother UNSAT. A split-at-head theorem preserves
  relative coverage, while an unconditional DNF cover is proved to imply the
  relative notion, so the previous API remains available.
- The ARM Lean build for `Ramsey55.CubeCover` succeeds. The relative UNSAT
  theorem has an empty axiom audit; split coverage uses only `propext`, with no
  `sorryAx` or `native_decide`. This matches the edge-pair cubes, whose exact
  counter cases cover formula-satisfying assignments rather than arbitrary
  assignments. Concrete sequential-counter semantics and DIMACS data import
  remain explicit next obligations.
- Added `Order45CubeCover.lean`. A closed-range Cartesian-product theorem
  proves that the generator's H-major/J-minor filtered lists contain exactly
  the edge pairs obeying the catalog bounds and excess thresholds. Kernel
  evaluation proves the concrete list lengths 28/36/45 with an empty axiom
  audit.
- Formalized the exact four-literal counter cube used by the manifest and an
  `ExactAtLeastCounterOutputs` contract. Under that contract, the d20/d21/d22
  theorems prove every allowed dense pair satisfies a concrete cube. The full
  ARM build now succeeds with 77 jobs; there is no `sorry` or `native_decide`.
  At this checkpoint the next narrow obligations were sequential-counter CNF
  soundness and the DIMACS variable-ID binding.
- Added `CnfCardinality.lean` and completed the generic sequential-counter
  soundness proof. The four local CNF cell shapes are proved equivalent to
  their Boolean recurrences, and a finite rows/width induction proves every
  observable state cell has the intended at-least meaning. The final-row
  theorem produces the bounded `ExactAtLeastCounterOutputs` contract without
  assuming nonexistent outputs past the generated width.
- Instantiated the counter-to-cube bridge with the actual order-45 dimensions:
  d20 H/J `(190,101)/(276,133)`, d21 `(210,108)/(253,123)`, and d22
  `(231,115)/(231,115)`. These theorems consume satisfied cell groups plus
  range/density bounds and produce membership in the concrete 28/36/45 cube
  families. Only DIMACS variable/subformula binding remains at this layer.
- Generalized the selective unified production script with a validated
  `RAMSEY55_PRIMARY_SPLIT_MAX` from 0 through 990 and a collision-free default
  output directory containing that value. This permits a fresh primary-990
  hedge whose preprocessing freezes graph variables but not tens of thousands
  of deterministic counter auxiliaries.
- Launched the three primary-990 selective unified hedges. Their logs bind
  initial freeze counts 1006/1008/1010 and the same 30k--128k, 1-second,
  10-second limits. The first snapshot was wider at 4/5/5 open nodes but only
  depth 2/3/3, which is the intended bounded structural alternative to the
  legacy auxiliary chain. Automatic mother-CNF replay and strict audit remain
  gated on runner status 20.
- Recoverably stopped the six legacy root-only leaf solvers after 6,740
  seconds. They occupied 16 GiB, none had closed, and together covered only 6
  of the 109 required top cubes, so they were not a complete certificate route
  without much more production. PIDs 3063694--3063699 are in `T` state; all
  partial proof/TSV files and waiting wrappers are retained and can be resumed.
  The legacy unified, both selective unified portfolios, and four fixed-pair
  solvers continue.
- Added `Order45Dimacs.lean`, formalizing the generator's row-major counter
  variable allocation from each post-lex base. Lean now constructs all signed
  DIMACS cubes from the formal pair lists and checks 28/36/45 lengths, the six
  manifest endpoint cubes, and final variables 78697/77148/76651. These
  numeric theorems all have empty axiom audits. The remaining data bridge is
  occurrence of the concrete input/state, bound, and sum clauses in the mother
  DIMACS stream.
- Added an explicit proof-fragment mode to the CaDiCaL cube runner. It closes
  the supplied cubes and flushes its trace without performing the final mother
  solve or adding an empty step. `tools/compose_binary_drat.py` atomically
  concatenates binary fragments and optionally appends that final empty step.
  The remote smoke test independently proved complementary children of one
  parent cube, composed the two streams, and passed `drat-trim`; the Python
  regression suite now passes 92/92 tests on the ARM builder.
- Took a non-destructive live checkpoint of the high-budget J326185 fixed-pair
  run after all first 16,383 roots had closed. Replaying the 353-row snapshot
  deterministically exports four open cubes. The copied 1,385,951,232-byte
  prefix has SHA-256
  `919985268d1bced620246d1e5ca15b6c17b518c35ed4c7b87585ccaa920c38d0`;
  the source process was immediately resumed. A separate ARM process is
  proving the four-cube frontier and is wired to concatenate and replay the
  complete stream only after it returns 20. Until that final replay says
  `VERIFIED`, this is a checkpoint experiment rather than a fixed-layer proof.
- Formalized the generator's complete truncated sequential-counter clause
  stream in exact row-major cell/clause order. Satisfaction of a mother CNF
  containing that substream now produces every local recurrence hypothesis;
  the associated axiom audit has no `sorryAx` or `native_decide`.
- Added d20/d21/d22 formula-relative cover theorems that consume the two H/J
  counter-substream inclusions and the range/density consequences directly.
  The targeted ARM Lean build succeeds. The remaining concrete data boundary
  is now the DIMACS input/state literal map, stream containment, and derivation
  of the bounds from the emitted unit and sum clauses.
- Proved the two range units sound and proved the full generated sum-clause
  family forces the H/J count threshold, including clauses with one omitted
  out-of-width literal. The combined constraint-tail theorem now supplies all
  five range/density facts without an external semantic hypothesis.
- Completed the concrete typed input/state map in `Order45Dimacs.lean` using a
  direct one-based DIMACS embedding with unused index zero. Lean reconstructs
  all six combinations-ordered input lists, checks their dimensions and
  endpoints, builds the exact H-counter/J-counter/constraint suffixes, proves
  those suffixes cover the 28/36/45 typed cube families, and checks every typed
  cube maps back to the signed DIMACS list. The full 79-target ARM build passes
  with no `sorryAx` or `native_decide`. The remaining DIMACS data obligation is
  suffix containment in each parsed/generated mother stream.
- Added independent single-cube fragment hedges for the third and fourth open
  J326185 checkpoint cubes, freezing only 36 and 18 input variables. The
  combined continuation and both hedges remain live and have found no SAT;
  none is counted as a proof before complete composition and replay.
- Rebalanced production before proof I/O could exhaust the 251-GiB remote
  volume. The primary-990 edge streams had widened to 47/60/58 open nodes,
  versus 2/1/19 for selective primary-0, so PIDs 3087105--3087107 were
  recoverably stopped. The fixed-pair 10k streams were at 12/11 open nodes and
  depths 309/441, versus 4/4 and depths 104/64 for the 100k streams, so PIDs
  3073107--3073108 were also stopped. The redundant root2-only fragment PID
  3092102 was stopped because the combined continuation is farther down the
  same hard cube. All wrappers and artifacts remain intact and resumable;
  nothing was deleted. About 102 GiB remained free at the decision point.
- Protected the legacy unified d20 proof before its depth-1024 hard limit. A
  brief `SIGSTOP` at a flushed 2,000-row snapshot copied the
  5,137,727,488-byte proof prefix, after which PID 3064085 was immediately
  resumed. The snapshot reconstructs three current-root cubes; adding the 27
  untouched pair roots gives a complete 30-cube remaining frontier. The
  prefix SHA-256 is
  `4849fbb423e15b17f6f5bc56ca7a6bb4e67a913aaf7aac2ae821d414433d84a4`.
  A selective fragment continuation is live with automatic composition and
  mother-CNF replay gated on exit 20; no proof is claimed yet.
- Extended `tools/export_proof_frontier.py` with
  `--include-later-roots`. It appends only untouched later roots and rejects a
  log in which any has started, making unified checkpoint recovery
  reproducible. A focused CLI test raises the ARM Python suite to 93/93; the
  hash-bound frontier pilot was updated to the new exporter hash.
- Replaced the proof runner's recursive DFS with an explicit LIFO stack. It
  preserves the signed positive-then-negative preorder and all solve/proof
  operations while removing the artificial depth-1024 failure mode exposed by
  legacy d20. The ARM binary
  `build/prove_cadical_cubes_explicit_stack_v5` has SHA-256
  `c05451ba6065ab49ead12c5c9a5926894d3fcd91b79916b8387727bf97d36057`;
  the full cube, root-only, fragment-composition, and independent
  `drat-trim` smoke sequence passes. Existing live binaries were not replaced.
- The old recursive d20 source PID 3064085 later reached depth 1024 and exited
  2 at its guard, as anticipated. Its earlier depth-952 prefix remains intact
  and the 30-cube selective checkpoint continuation is live; therefore no
  completed proof work before the checkpoint was lost. No certificate claim
  uses the failed post-checkpoint tail.
- Added `tools/audit_order45_counter_tails.py` and the hash-bound
  `data/order45-counter-tail-manifest.json`. Without altering the production
  formula manifest, the auditor verifies each full mother hash, seeks to the
  exact suffix offset, reconstructs every H/J counter, range-unit, and sum
  clause, compares 167,810/161,604/159,612 clauses, and binds the six ordered
  input-ID lists. The three suffix hashes are
  `dd952b03d59f669e2b65e21d7bb86e961af30fb3a301ad69004b7c3c29895b4b`,
  `7b81acea3af0dc44b7c38dc6a47112ec12100ec95f2147fb186b1174708d4e75`,
  and `47525d3fd7eabd58f2023687527ff4cbb02772088136b4cbbcd28fe86e8c1b10`.
- Made the edge-strata and lex independent verifiers importable both as
  scripts and `tools.*` modules. The original full three-formula verifier still
  passes, the new tail auditor passes, and the ARM Python suite is 96/96.
- Split the d20 checkpoint's 30-cube residual family into the current root0,
  two other deep snapshot cubes, and three ordered nine-cube blocks covering
  the 27 untouched roots. `tools/audit_icnf_partition.py` independently checks
  exact ordered concatenation and emits a hash-bound partition manifest; the
  whole frontier hash remains
  `4facb09ec98441fec5b2257ac7922b17b7742018d8c69844f94c75234a3fa3b2`.
  The explicit-stack fragments are running in parallel. The superseded
  29-root and 27-root combined processes were recoverably stopped with their
  artifacts retained. Two focused tests raise the ARM suite to 98/98.
- Tested whether larger CDCL budgets would close the current hard cores. The
  J326 snapshot had two 1,000,000-conflict UNKNOWN calls taking 86.075 and
  113.333 seconds, followed by one incrementally learned UNSAT call; the d20
  hard cube remained UNKNOWN after 1,000,000 conflicts and 387.771 seconds.
  A 120-second materialized-CNF portfolio also left every d20 configuration
  UNKNOWN, although default Kissat reached 867,773 conflicts and was about
  2.8 times faster per conflict than the assumption scan. This rejects simply
  raising every CaDiCaL node budget to one million conflicts.
- Re-audited global rather than current-root frontier counts. The original
  fixed-pair streams were at four and three open nodes, while the duplicate
  J326 checkpoint had widened to four; selective d22 had widened to 109 nodes
  versus 54 for legacy d22. Recoverably stopped the two dominated J326
  checkpoint children and selective d22 with `SIGSTOP`; no artifacts were
  deleted. The 64-core machine had ample CPU, while proof-log I/O and an
  87-GiB disk margin were the operational constraints.
- Added the materialized leaf-certificate path. The producer appends each cube
  as units, accepts UNSAT only after immediate `drat-trim`, and hashes the
  formula/cube/augmented-CNF/solver/checker/proof/log boundary. The independent
  auditor rebuilds every augmented CNF and replays each proof again. A real
  two-leaf Kissat smoke returned 20 twice and both independent replays said
  `VERIFIED`.
- Added proof-forest snapshots that reconstruct all closed leaves and open
  nodes from one immutable TSV byte snapshot. An independent per-root DPLL
  audit proves the leaves refine every source root. The J297/J326 snapshots
  contain 16,750+4 and 16,756+3 closed/open leaves; their manifest hashes are
  `c45e0f4c415e94cc499e3120a55146bd7064bae9588a95fb16cea79be31ba013`
  and `f374ed7fab802b4432e7b28f83c0de82c9c1f1cbedb79107bdaad30ead64ea7c`.
- Added a linear sibling-merge cover certificate and replay checker. Both
  fixed-pair depth-14 families reduce 16,384 roots to the empty cube in 16,383
  checked merges. Certificate hashes are
  `9751264998b8b290547d52f5cea2538d8f7954b48aad8132bebcab56a25dcc6c`
  and `ee1baddd1e031afbc2f15ba19c245440b5b2b010ee73d890a6a234933d0ba8e8`.
- Materialized proof-mode reruns closed and independently replayed two of four
  J297 open cubes and one of three J326 cubes. Audited lookahead refinements
  replaced the four residual parents by complementary child pairs; proof mode
  then closed one child per parent in about 0.023 seconds. Added a resumable
  driver that iterates audited UNKNOWN extraction, binary refinement, DRAT
  production, replay, and atomic state updates with a 20-second budget. Both
  open chains and the two large closed-leaf proof collections remain in
  progress; none is yet a complete fixed-pair or order-45 certificate.
- The ARM test suite passes 107/107 after adding the forest, materialized DRAT,
  UNKNOWN-frontier, binary-refinement, and fast cover-certificate tests.
- The J326185 closed-leaf materialized batch finished with 16,746 independently
  checked UNSAT leaves and ten UNKNOWN leaves out of 16,756. A separate
  48-worker audit rebuilt every augmented CNF and accepted all 16,746 retained
  DRAT proofs. Its certified residual chain reached four UNKNOWN leaves before
  the next scheduling change. The older dominated unified and fixed-pair
  streams remain recoverably stopped; their artifacts were retained.
- Uniform 20- and 60-second residual rounds exposed a repeated
  one-easy/one-hard sibling pattern. Added an optional staged policy to the
  materialized chain: prove all children for two seconds, then spend 60 seconds
  only on parent pairs whose two children both remain UNKNOWN. Added
  `compose_materialized_cube_proofs.py` to replace only an ordered UNKNOWN
  subsequence, bind both source manifests and replacement indices, and carry
  hash-checked proof artifacts into a fresh combined manifest. A real ARM
  two-cube composition smoke passed the final independent `drat-trim` replay.
  The J326 closed residual chain switched to this policy at round 20; the
  J297775 and J326185 open chains switched at audited round boundaries 33 and
  36 respectively.
- J297775 closed-leaf production finished with 16,734 UNSAT and 16 UNKNOWN
  results out of 16,750, with no SAT result. A separate 48-worker audit rebuilt
  all 16,750 augmented CNFs and accepted all 16,734 DRAT proofs. Its staged
  residual chain then started from the independently audited manifest.
- The complete local and ARM Python suites both pass 112/112 after the staged
  retry, ordered materialized-proof composition, exact retry-file, tool-binding,
  and chain-refinement tests.
- Added an independent whole-chain auditor. It snapshots `state.json`, replays
  every materialized manifest, reconstructs the exact ordered UNKNOWN frontier,
  verifies every complementary binary refinement and its hash-bound manifest,
  and checks that each next proof manifest binds the exact child file. Also
  closed a provenance gap in the leaf auditor by checking the recorded solver
  and checker binary hashes and requiring the supplied checker to match.
- A real J326185 open-chain snapshot replay passed from first round 2 through
  final round 45: 44 proof manifests, 43 complementary-refinement rounds, and
  188 refined parents were accepted. The terminal partial manifest still had
  seven verified and seven UNKNOWN children. The audit-manifest SHA-256 is
  `ec1c1561a1a2b628031c2b1c3adee5cbc1ca8b32b4000632cb173da2963508bc`;
  this is chain-integrity evidence, not a completed fixed-pair proof.
